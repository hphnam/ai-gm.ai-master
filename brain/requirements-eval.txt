# Evaluation-only dependencies for the PRJ93 fidelity corrections. NOT runtime
# dependencies: the pipeline runs without these; the corresponding probes and
# cross-checks self-skip (and say so) when an import fails.
#
#   pip install -r requirements-eval.txt
#
# Notes:
# - statsforecast is the Croston/SBA cross-check oracle (WP2 G2.2). It does not
#   build on Python 3.14 (scipy/numba); the cross-check test skips with a reason.
# - chronos-forecasting + torch (CPU) drive the Rung-4 zero-shot eval (WP4). The
#   chronos-bolt-small weights download from Hugging Face on first use.
# - TSB-AD provides the pinned VUS-PR metric (WP5); vus is the pinned fallback.

statsforecast>=1.7

chronos-forecasting>=1.4
torch>=2.2

TSB-AD>=0.2
vus>=1.0
