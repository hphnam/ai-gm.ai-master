# PRJ93 log folder

Consolidated build reports, experiment reports, and the decision log for the
Proactive Brain (Track A). Files are numbered in order of implementation, traced
by each report's first-commit date across the project history. The `PRJ93_`
prefix has been dropped. Two provenances are gathered here:

- **Archival set** (originally at repo root on `feat/chronos2-promotion`): the
  WP1 to WP12 reports and the decision log.
- **Working-trunk set** (authored on `brain-construction`): the G12.9 / G12.10
  reports and the downstream rerun matrix.

## Reports, in implementation order

| # | File | First commit | Scope |
|---|---|---|---|
| 01 | `01_Phase2_Build_Report.md` | 2026-06-19 | Phase 2 build |
| 02 | `02_Phase2_Remediation_Report.md` | 2026-06-22 | Phase 2 remediation |
| 03 | `03_Build_Report_Current.md` | 2026-06-25 | Consolidated current build report |
| 04 | `04_ChangePoint_A13_Report.md` | 2026-06-29 | A13 change-point detection |
| 05 | `05_PointDeviation_Report.md` | 2026-06-29 | Point-deviation primitive |
| 06 | `06_Proactive_Briefing_Report.md` | 2026-07-01 | Proactive briefing capstone |
| 07 | `07_Live_Ingest_Report.md` | 2026-07-01 | Live-ingest / freshness / conditional retrain |
| 08 | `08_Promote_And_Serve_Report.md` | 2026-07-01 | Promote-and-serve (v2.1) |
| 09 | `09_Agent_Eval_Report.md` | 2026-07-03 | Agent evaluation framework |
| 10 | `10_Stock_Part2_Prefill_for_James.md` | 2026-07-03 | Stock Part 2 prefill (handoff) |
| 11 | `11_Scaled_Eval_Report.md` | 2026-07-06 | Scaled evaluation |
| 12 | `12_WorldCup_LiveProbe_Report.md` | 2026-07-06 | June 2026 live probe vs the withheld World Cup |
| 13 | `13_LiveIngest_Fixes_Report.md` | 2026-07-07 | Live-ingest fixes |
| 14 | `14_Fidelity_Corrections_Build_Report.md` | 2026-07-07 | Fidelity corrections build |
| 15 | `15_Fidelity_Corrections_Addendum_Report.md` | 2026-07-08 | Fidelity corrections addendum |
| 16 | `16_Chronos2_Promotion_Report.md` | 2026-07-08 | WP12 Chronos-2 promotion |
| 17 | `17_G12_9_Report.md` | 2026-07-08 | G12.9: fold unification, Ellel uncap, weather precision |
| 18 | `18_DOWNSTREAM.md` | 2026-07-08 | G12.9g downstream rerun matrix |
| 19 | `19_G12_10_Report.md` | 2026-07-08 | G12.10: TRT coord, is_ellel_event leak, full exo set, World Cup, Neon adapter; plus the G12.11 Ellel narrative correction and decision-log rows |
| 20 | `20_G12_12_GoLive_Forecast_Report.md` | 2026-07-09 | G12.12: go-live forecast attempt; clean STOP at gate a (June absent from store, Neon not provisioned) |
| 21 | `21_G12_13a_Frozen_Forecast_Report.md` | 2026-07-09 | G12.13a (Pass 1): forward June 2026 forecast frozen and pre-registered, blind to actuals |
| 22 | `22_G12_13b_June_Simulation_Report.md` | 2026-07-09 | G12.13b (Pass 2): frozen forecast confronted with real held-out June actuals (MCP-SIM); full brain run over June; leak-free |
| 23 | `23_G12_13_Canonical_Reconciliation_Report.md` | 2026-07-09 | G12.13 reconciled against the canonical Pass-1/Pass-2 specs; measured A-vs-B split closed (MinT wins BH/TRT L2/L3); provenance + reasoned .md added |
| 24 | `24_G12_15_Report.md` | 2026-07-10 | G12.15: Chronos on MPS (parity, CPU faster for small runs), home-nation fixture flags (Scotland matters), cadence sweep (BH 7-day sweet spot), event-aware refresh policy, stock flagged |
| 25 | `25_G12_16_Report.md` | 2026-07-10 | G12.16: Square-to-brain taxonomy map (`ingest/taxonomy_map.md`); mapped L2 re-score (unchanged); item-grain June pull (reconciles GBP 0.00); first real L3 item MASE (BH median 1.33, Ellel 0.24); finding is taxonomy drift not name misalignment (FLAG-TAXONOMY-DRIFT) |
| 26 | `26_G12_17a_July_Pass1_Report.md` | 2026-07-10 | G12.17a (Pass 1): advance clock (June ingested), liveness gate (TRT dormant), taxonomy refresh (LuneBrew Pilsner now tracked), June-inclusive refit (robust-DOW edges Chronos-exo, not adopted), frozen blind July 1-7 forecast committed as pre-registration |
| 27 | `27_G12_17b_July_Pass2_Report.md` | 2026-07-10 | G12.17b (Pass 2): July 1-7 confronted (BH L1 MASE 0.39, beats backtest class); in-context test (1 Jul lifted above baseline in anticipation, single-case); drift decomposition (Ellel named coverage +38pp); brain over July (0 new-items/week fatigue, TRT no alarm spam, liveness gate confirmed vs reality) |
| 28 | `28_G12_17c_July_Window2_Freeze_Report.md` | 2026-07-10 | G12.17c (Step C1): freeze a second blind July window (8-14 July) from the same June-inclusive cutoff, carrying the England QF (11 Jul, in-hours) + two generic matches; airtight-by-calendar pre-registration (11-14 Jul still future); BH lifts every match date, England most (+GBP 312 vs generics); flags asserted; deviation: Ellel's late Saturday window fires its 11 Jul England flag (inert, robust-DOW reads no wc). Step C2 (confront) awaits after 2026-07-14 |
| 29 | `29_G12_17cb_Corrected_Freeze_Report.md` | 2026-07-10 | G12.17c-b (Corrected C1): advance the clock through 7 July (ingest 1-7 July as observed history) and re-freeze 8-14 July from the 7-July cutoff (Origin B, true 7-day horizon) alongside the untouched Origin A; same models, differ only in cutoff. Honest null: the extra week does NOT sharpen the 11 Jul England anticipation (+GBP 309 vs A's +312) but RAISES the generic-match anticipation (9 Jul +32, 10 Jul +57), narrowing the home-nation premium; C2 scores both origins after 2026-07-14 |
| 30 | `30_G12_18_Comment_Rewrite_Report.md` | 2026-07-10 | G12.18: edit-in-place pass over `brain/**/*.py` comments and docstrings via the avoid-ai-writing skill (technical voice). Vocabulary already clean; the one tell at scale was the em-dash separator. 212 spaced dashes rewritten to punctuation across 53 files (comments + docstrings only); 159 runtime-string dashes and all unspaced numeric/gate ranges preserved; 0 non-py files touched; both suites green. No confirmed stale notes; glossary drafted with a placement question |
| 31 | `31_G12_17c_C2_Confront_Report.md` | 2026-07-16 | G12.17c (Step C2): confront BOTH 8-14 July origins with held-out actuals (pulled 2026-07-16, after the window closed - airtight by calendar). Accuracy holds: BH L1 per-day MASE 0.285 (A) / 0.287 (B), beating the 1-7 July 0.386, band coverage 1.00 (the 11 Jul +GBP 574 miss stayed in-band). Origin B does NOT beat Origin A - an honest null corroborating report 24's cadence sweep. **The pre-registered England anticipation FAILS:** both origins expected ~+GBP 310 on the 11 Jul QF; realised -GBP 265, wrong in sign, so the two-case record is 1 for 2. The kickoff-time explanation is REFUTED by 27 Jun (same Sat, same 22:00, +234%, the largest lift on record); the shortfall is unexplained by `CHRONOS2_EXO_COLS`. Supersedes report 24's "England +130%, generic within noise". TRT dormant, no false alarm (3rd confirmation). Caveat: Ellel's 0.096 MASE is an artefact hiding a 90.2% under-forecast |

| 32 | `32_G13_Production_Integration_Report.md` | 2026-07-16 | G13: prepare the engine to run as a per-org service inside gm-ai. Ryan's integration brief verified claim-by-claim: its central call (stateless compute, API owns persistence) is right and adopted; three claims are wrong, and the worst one traces to THIS project's stale `FLAGS.md`, not the reader — `7d8bfbd` scoped the A14 verdict everywhere except the live ledger, and the brief read the ledger. Hardening: bearer auth (bytes compare — the str form 500s on a non-ASCII token), secure-by-default posture after the first cut **failed open** on a typo, `/docs` nulled, `POST /refresh` deleted (honest remainder: `?freshness=live` still writes, now bounded). Stateless compute built on a per-request scratch seam rather than rewriting ~157 call sites; Neon/Square adapters + psycopg **deleted, not disabled** (retiring finding L3 by removal). The isolation claim survived an adversarial review that could not falsify it. Two bugs found by building: an org with no sales 503'd (the first thing a new tenant hits), and the contract accepted covariates then dropped them silently. Suites 307/8 and 314/1; **C2 reproduces bit-for-bit** |
| 33 | `33_G14_De_Lune_Report.md` | 2026-07-17 | G14 (Phase 3): the de-Lune pass report 32 §6 deferred — but the headline is what building it found. **Compute did not forecast.** `_forecast_venue` drained `conformal.wrap.evaluate`, which is a BACKTEST: 57 rows returned, 57 for dates already inside the supplied history, **0 after it**; the API would have persisted banded, model-named predictions about days that had already happened. Phase 2's own diagnostic ("horizon_days NOT honoured; the analytics emit their own horizon") was true and misleading — there was no horizon — the exact failure `_report_unconsumed` exists to prevent. `compute/forward.py` fixes it. The seam (`org_profile.py`): UNBOUND = Lune's `config.py` so the research path reproduces; BOUND = the profile **entirely**, since a per-field fallback would hand Lune's Mon/Tue closure to a seven-day tenant *through the Mondrian grouping* — a miscalibrated band, not an error. Exposed a latent bug: the grouping read the **literal** `(0, 1)`, so `STRUCTURAL_ZERO_DOW` never reached the band it defines. Two Lune reads were silent wrongness not crashes (`NaT` poisoning `max()` made `is_closed` False for every tenant **by accident**; the hardcoded `"ellel"` slug zeroed a tenant's spillover). Closed the hole under `extra="forbid"`: `values` is a free dict, so `exo_tempc` validated and did nothing. Three CONTRACT.md claims corrected — incl. `is_event_driven` never capping the rung (stale by a fortnight; same species as Ryan's error, this time in our own contract) — and the weather decision **reversed against the contract's own recommendation**. Honest correction: C2 **re-scores a frozen artefact**, so report 32's "reproduces bit-for-bit" never proved forecast generation; the real gate is the training-frame hash, byte-identical on all three venues. **Review found seven defects and three were this module breaking its own stated rule** — both reviewers independently found the worst first: `exo_is_dry` **inverted between train and serve**, feeding Lune's flagship served entrant a covariate that read 0 in training and 1 at serving, silently. Plus: a band calibrated on ≤7-step errors applied to day 30 (a caveat `sim/` documents and the port dropped), 13 columns missing so `rung3_gbm` raised KeyError, `exo_enabled` gating only half the pipeline, and (security, HIGH) one typo'd year in a POS export → **~9,272 re-fits from a 2-row request**. All fixed and re-measured — and a second pass found that two of those first fixes were the WRONG fix: bounding the typo'd-year DoS left it silently forecasting **January 2202**, and the band caveat was a diagnostic where a refusal belonged (`horizon_days` now capped at 7, the horizon the band is actually calibrated for and the only one this project evidences). A third pass was needed because that fix was ALSO wrong, and both reviewers caught it independently: the span guard rejected `2202` and **accepted `2026→2027`** — a likelier typo — and could not see a wholly mis-stamped export at all. `sales_daily` is closed history, so the guard is now absolute (any row after today), not relative. Suites 370/8 and 377/1 |
| 34 | `34_G14b_Defect_Closure_Verdict.md` | 2026-07-17 | G14b: the defect-closure ledger and verdict for Phase 3. All 22 defects closed and re-measured — but the number that matters is that **three consecutive rounds of my own fixes each contained a defect as bad as the one being fixed**, and review found all three. The `exo_is_dry` fix made train and serve agree on the wrong value; the DoS fix turned a 17-minute hang into a 0.4-second wrong answer; the guard for *that* rejected `2202` and accepted `2027`; and its replacement rejected every forward typo and accepted **every backward one** — `2026→2016` makes `rung1_robust_dow` return **£0.00 across the horizon**, banded, named, no error. Each was plausible, confidently documented, tested, and measured only against the case that motivated it. What worked was review run against the FIXES rather than the code. Open by design: FLAG-BAND-HORIZON (per-step conformal needs its own gate) |
| 35 | `35_For_Ryan_Integration_Brief_Response.md` | 2026-07-19 | For Ryan: the brain-side response to `BRAIN_PRODUCTION_INTEGRATION_BRIEF.md`, answered §1→§11 in his own order, each point tagged **Adopted** / **Deviated** / **Yours**. His central call (stateless compute, API owns persistence + tenancy) adopted; brain side of Phases 1 and 3 done, research numbers intact. The headline for the persistence work: **Phase-2 compute returned a backtest, not a forecast** — don't wire `brain_forecasts` against that shape. The two disagreements he asked to be explicit about, both in §6.1 and both would have silently degraded the served Chronos-2 model: (1) "nothing reads intraday detail" is false (`derive_trading_hours` reads `ts`; six World Cup covariates depend on it) — handled by agreeing the aggregate grain and having the API supply `trading_hours` directly; (2) "weather/events are attribution-only, rejected by A14" is false (A14 binds only the GBM; the served `rung4_chronos2_exo` uses all 15 exo cols incl. weather) — **but this was our stale `FLAGS.md`, not his code-reading**, fixed in `a4e5fa3`, and the per-org exo *gate* he wanted is still right, just for tenant-appropriateness not because weather is attribution-only. Three deliberate deviations from his §7.2 (VAT → brain is VAT-agnostic; `timezone`/`currency` removed as fields-that-do-nothing; `sim/` → `.dockerignore` not a move). Residuals surfaced honestly: `PRICE_REGIME_BREAK` still Lune-hardcoded and reachable on the tenant path; the two `@lru_cache`es deliberately un-keyed until org threads through the serve surface; **§6.4 — the re-fit ladder never re-runs and `_should_refit` isn't wired to injected `prior_state`**, so a nightly cron built on "always learning" would loop and persist an unchanged selection. Plus the 11-point caller handshake and a `67→263 print()` correction |
| 36 | `36_G15a_Fixture_Shortfall_Diagnostics.md` | 2026-07-19 | G15a: the 11 July shortfall diagnosed. Report 31 named three hypotheses and both testable ones came back **negative**, so the shortfall survives diagnosis. All POST HOC (the actual was seen first). **Weather refuted** by the same 27 June control that killed kickoff time: 11 Jul was 2.1 C warmer, 0.9 hrs sunnier and equally dry than the largest lift on record. **The finding in no report: `is_ellel_event` is a train/serve asymmetry** - one of the 15 served exo cols, populated from observed trading, **pinned to 0 on every horizon**, so the model is fit on a covariate informative on 66 of 399 days and constant on every day it serves. Same species as report 33's `exo_is_dry`, documented rather than hidden. Direction is **substitution, not spillover**, refuting Lune's own hypothesis as written into the source (fourth refuted belief). **The estimator trap:** pooled reads **+GBP 500**, DOW-matched **-GBP 23** - a sign inversion from the estimator alone, because Ellel books weekends and the Beer Hall is a weekend venue; the naive version confirms what the matched version refutes. The counterfactual **bounds** rather than supports: 1558.28 to 1530.78, **GBP 27.50 of GBP 573.66, 4.8%**. Deviation that mattered: the spec's constant-flag arm reports **+47.37** on 11 Jul where the single-date spike reports **-27.50**, opposite signs, because Chronos-2 conditions on the whole path. Control arm reproduced Origin B **to the penny** (0.00 gap, one venue-day) - first evidence forecast GENERATION is reproducible from the store at all; scoped down from "bit-reproducible" by G16b.1. Events arm **untestable** (203 rows, 2 dates). `EXCLUDED_VENUES` dead and **propagating a false claim into a committed artefact**; deleted, artefact deliberately left unedited. Limitation 4 was **overstated** and is corrected. **And the hash gate was never runnable**: the three published prefixes exist in no script in any commit, `ellel` is 392 x 40 not 386 x 40; `sim/frame_hash.py` committed with a re-measured baseline |
| 37 | `37_G15b_Round4_Review.md` | 2026-07-19 | G15b: round 4, the review report 34 flagged as unrun. **It was not clean, so the record is four rounds and four with hits** and the yield curve has still not turned over. The severe one is the same shape as the previous three: the round-3 isolation guard ran on the **pooled** dates of the whole request, though every word of its reasoning and all of the harm is **per venue**. Measured with one identical typo row: single venue **REJECT**, plus a sibling spanning the hole **ACCEPT**, plus a sibling of **one row every 60 days ACCEPT** - 64 rows defeat it. So the guard built to stop a poisoned forecast **was inert on every multi-venue org**, which is every real one; Lune has three. Fixed per venue, and the fix's own blast radius measured and reported (it removes the accidental cover pooling gave pop-ups and reopenings: two regressions, stated). Second defect exactly as predicted: the band guard read `MAX_HORIZON_DAYS > _CALIB_BLOCK_DAYS`, firing on the tested path and **not the symmetric one** - raising the other symbol changes the **banding method** under an unchanged horizon, silently and toward safe over-coverage, so nothing fails; now `!=`. A third defect was **measured and deliberately left open**: a venue reopening after 8 months is refused for its first 13 trading days and takes every sibling down with it, but the obvious trailing-segment fix was measured and **relights a dormant venue**, so an honest open item beat a fourth wrong fix. `PriorState` bounds the LIST not the CONTENT (100 MB accepted, echoed verbatim) - **recorded and stopped**, contract surface Ryan has not built against. Three findings did not survive contact and are reported at the same prominence. Suites 385/8 and 392/1, all six new tests verified to fail pre-fix |
| 38 | `38_G15c_Taxonomy_Drift_Decision.md` | 2026-07-19 | G15c: the taxonomy-drift question closed, and **the answer is do not wire**. `since=` is confirmed unwired on the standing path. Gated like a rung promotion with a blind before/after, and refused on three grounds. Beer Hall L3 revenue MASE **0.852 to 1.08-1.16** at every lookback - from beating seasonal-naive to losing to it; the two venues move in **opposite** directions on capture (BH 19.3 to 29.0%, **Ellel 31.3 DOWN to 15.3%**). **And the prescription does not fix the symptom it was prescribed for**: `LuneBrew Pilsner`, report 25's named smoking gun, is **never selected at any lookback by either ranking** - rank 5 at best under a 56-day window and still outside a top-3. The binding constraint is **`top_k`, not the ranking window**, so the open question had a false premise; wiring it would have raised capture, read as progress, and left the item in OTHER - the count-check trap one level deeper, findable only by testing the fix against the specific case rather than the aggregate. `top_k` was not raised either: it is the next experiment, not a fix to smuggle in. Underneath sits a metric finding: refreshing swaps stable long-history lines for recent ones whose base forecast is worse AND whose seasonal-naive denominator is smaller, so **MASE is punished twice and the node set that scores best is the node set that matters least**. Sibling to FLAG-MASE-INTERMITTENT; together they say the gate the ladder rests on is **blind to relevance**. New-item revenue kept separate throughout (**12.6% BH, 42.7% Ellel**, irreducible). Report 25's 26/15% updated to 19.3/31.3% on a **different hierarchy** (frozen revenue-ranked vs standing units-ranked), never previously compared |
| 39 | `39_G15d_Price_Regime_Seam.md` | 2026-07-19 | G15d: the last hardcoded Lune date on the tenant path. `PRICE_REGIME_BREAK = "2025-07-01"` was stamped into **every** org's feature frame by `build_features` - Phase 3's de-Lune table missed it. Same species as report 33's Lune reads: not a crash, a **plausible wrong number** (a free split point for `rung3_gbm`, a covariate for the Chronos exo entrant, nothing said). Closed with `OrgProfile.price_change_dates`, additive and optional, the feature generalising from a binary flip to a **count of changes preceding each row** - the smallest generalisation that carries a list, degenerating exactly to the old column at n=1, which is why the three frame hashes are byte-identical **after** the change. Bound-with-empty-list gives a flat column, verified end to end and not just at the accessor, because "empty means none, not unset" is the rule the seam rests on. Bounded at 100. Suites 391/8 and 398/1. Ruff not run: absent from both venvs, so its counts are unverified here |
| 40 | `40_G16_Portable_Baseline_and_Corrections.md` | 2026-07-20 | G16a+b: the hash gate made **portable**, and four corrections. Report 36 recorded the pre-de-Lune comparison as permanently lost; it was not, so it was rebuilt. Both trees measured against **one copied store** through **one interpreter** by a shim **validated to reproduce the current gate first**, at `2cc97e7` (no `org_profile.py`, 205 changed lines ago in the feature builder) and `c008651`: **all three frames identical** (`8c8a8be9d8dc5791` / `b6339032a219213c` / `ea28bcacbf1825e4`, 399 / 331 / 392). **1,072 inserted lines across the feature path, zero movement in any frame.** Outcome 1 of three; the STOP outcome did not occur. The de-Lune is now verified **portably** rather than session-locally, which is a stronger claim than report 33 could make. `ellel` 386-vs-392 confirmed **by arithmetic**: 2025-06-08 to 2026-07-04 is 392 days, a 2026-06-28 ceiling gives 386, and the six-day gap is exactly the two July W1 rows. Reports 33/34/35 annotated at each hash table, dated, values left in place; **35 gets the fullest, because it went to Ryan as the reason to trust the de-Lune**. Four corrections: **"bit-reproducible" was an over-claim** (agreement to the penny on ONE venue-day, the same species report 33 corrected report 32 for, repeated in the report congratulating itself for catching stale claims); the numbers-to-quote table's stale 26%/15% row split into two rows with bases named, table audited in full, **pooled +GBP 500.18 added to do-not-quote**; ruff resolved by **measurement not marker** (70 under **ruff 0.15.22**, matching report 34; report 39's "not installed" was an under-investigation since `uvx` was always there). **And one correction was itself wrong:** the spec claimed the D3 pinning test could never have failed pre-fix; measured, **all six fail**, but four on BEHAVIOUR and two on the ERROR STRING only, so report 37's four is exactly right and the real lesson is sharper than the spec's, that **a test failing pre-fix on a message is not evidence a defect was fixed**. G16c: caller obligations moved to `CONTRACT.md` as canonical, plus a 12th (a reopening venue fails the whole request) and the admission that **D1's fix made it more likely, not less** |
| 41 | `41_For_Ryan_Addendum_Post_G15.md` | 2026-07-20 | Addendum to report 35, for a reader who has report 35 and nothing else. Four things went stale and three affect what he builds. **(1) The trust gate he was given was not runnable** - the three hashes report 35 leads with exist in no script in any commit, so he decided on unreproducible evidence; G16a's portable measurement now makes the claim true on better evidence than when he accepted it. **(2) Round 4 was run and was not clean: four rounds, four with hits.** The severe one: the round-3 isolation guard validated the **pooled** dates of the whole request, so it **did not fire on any multi-venue org** - 64 sibling rows defeat it, and Lune has three venues against a cap of 25, so it protected exactly the configuration nobody runs. **(3) A new availability behaviour that is his:** one reopening venue fails the whole request (FLAG-SEGMENT-FALSE-REJECT), open by design, and **D1's fix made it more likely by removing an accidental mask** - from where he stands, a regression. **(4) Two contract-sync inputs, not changes:** `PriorState` content unboundedness and the validator's wall-clock dependency. Plus what did NOT change, stated so he need not infer it: no dissertation number moved, served models untouched, decision 6 still open and still his before the nightly cron, all report-35 obligations stand. One line **marked as a judgement, not a fact**: on four of four, this codebase's fixes are not converging, and the G15b fixes should be treated as unreviewed on the same evidence |

`Decision_and_Resolution_Log.md` is intentionally un-numbered: it is a
cross-cutting, append-only record spanning WP1 to WP12, not a single
implementation step.

## Ordering caveats

The order is by first-commit date, which mostly matches logical implementation
order but diverges in a few late-committed docs (several reports for earlier work
were committed in a burst on 2026-07-06 to 08). Notably, `12_WorldCup_LiveProbe`
is commit-dated before `16_Chronos2_Promotion` even though the live probe builds
on the promoted Chronos-2 model. Re-order by hand if a strict work-package
sequence is preferred over commit date.

## Decision log coverage

`Decision_and_Resolution_Log.md` is the continuous WP1-to-present decision log.
Section A covers the fidelity corrections, Section B the WP8 to WP12 addendum and
Chronos-2 closeout (rows 1 to 5), and Section C the post-WP12 milestones: G12.9 to
G12.11 (rows 6 to 10, reconciled in from `17_G12_9_Report.md` and
`19_G12_10_Report.md`), G12.12 (row 11), the G12.13 June confrontation (row 12
G12.13a pre-registration, row 13 G12.13b out-of-sample result), and the G12.17 July
work (row 17 G12.17a Pass-1 freeze, row 18 G12.17b Pass-2 confront, row 19 G12.17c
Step-C1 second-window freeze, row 20 G12.17c-b corrected 7-day-cadence re-freeze).
The three G12.11
rows the G12.10 report labels B6/B7/B8 are Section C rows 8, 9, and 10.
`brain/FLAGS.md` remains the live flag ledger.
