# Margin spills that have been RULED and may pass brain/scripts/formatcheck.py.
#
# Format:  <max_pt> <substring of the offending text>
#
# The cap is a CEILING, not a label. The same defect getting worse is a new
# defect and still fails the gate. Delete a line the moment its defect is
# repaired -- an accept file nobody prunes is an accept file that hides the
# next regression.
#
# Nothing goes in here without a ruling recorded in the decision log or in a
# ledger entry. "It has always been there" is not a ruling.

# REPAIRED 2026-08-12, line removed per the pruning rule above. The Zotero
# translator artefact `note = {\_eprint: https://...}` was deleted from ref.bib,
# where it stood SIX times and not once -- the accept line named only the Hansen
# entry because that was the only one long enough to spill. Deleting all six also
# cleared the 88.49pt overfull hbox at main.tex:384, which had been carried as a
# separate finding since 2026-08-11: one defect wearing two instruments' labels.
# Recorded in ledger/final_audit.md section 7 (D-6).

# appendix/project_specification.tex, a line breaking inside "answers staff
# questions on WhatsApp". TeX reports NO overfull box against it and
# \emergencystretch=2em did not clear it. Raised 2026-08-11, ruling pending.
#
# STILL LIVE after the 2026-08-13 source swap, which is the point of keying on
# text rather than on a page: that appendix now reproduces the Week 1
# specification instead of the project advert, and the same clause spilled again
# at a different page and a different width (3.61pt then, 3.51pt now). Both sit
# under the ceiling. Delete this line if a future edition of that sentence
# breaks elsewhere.
5.0 staff
