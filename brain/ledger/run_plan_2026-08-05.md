# Consolidated run plan — 2026-08-05

Written under `brain/PRJ93_RULES.md`. Companion to
`brain/ledger/literature_conformance.md`. **Nothing here has been run.** This is
gate 3 (rerun) and gate 1 (methodology change) material; every item waits.

Sources, per the brief: **(a)** implied by the methodology but never run ·
**(b)** ran but lacks the data points to support a defensible figure ·
**(c)** invalidated by the fidelity audit in `code_vs_paper.md` ·
**(d)** required to close a DIVERGES — SHOULD FIX row.

Runtime estimates are anchored on the only two figures the reports record:
ladder regeneration ~37 min of Chronos time (`log/43:224`) and the weather
diagnostic ~40 min (`log/57:84`). Non-Chronos evaluation paths are minutes.

---

## Ranked by marginal value to the dissertation

### R1 · VUS-PR detector supplement — **run first**
- **Source** (a) + (d), closes **D-F2** and weakness **W25**.
- **Purpose** The review commits to a lag-tolerant VUS-PR-style detection metric and explicitly rejects point-adjusted F1. No committed artefact carries a VUS-PR number.
- **Keys** `liu_elephant_2024` (*"VUS-PR emerges as the most robust … accurate … and fair evaluation measure"*), `kim_towards_2022`, `bhattacharya_towards_2024`, `gim_evaluation_2023`.
- **Code change** **None.** `eval/agent_eval.py:563 vus_pr_supplement` is implemented and calls the pinned library rather than reimplementing the metric.
- **The blocker was wrong** Report 11 records *"not computed, dependency unavailable"*. Verified this session: `.venv-eval` imports both `vus` and `TSB_AD`. `.venv-run` and `.venv-forecast` do not — which is why it kept reading as unavailable.
- **Command** `.venv-eval/bin/python -m eval.agent_eval`
- **Runtime** ~5–15 min (no model calls, no Chronos).
- **Unblocks** The W25 broken promise; lets the Results chapter lead on a defensible detection figure instead of the 0.996 recall the review's own R42 disqualifies as a headline (**D-F8**).

### R2 · Chronos-2 covariate probe, re-instrumented — **highest single credibility gain**
- **Source** (b) + (c) + (d). Closes weakness **W37**, which is `[O]` and rated **Major**.
- **Purpose** `eval/chronos2_covariate_probe.md:15` currently concludes **"Outcome: covariates HELP"**. The table above that line is 3 folds better, 3 folds worse, mean delta −0.014 against a per-fold spread of roughly 0.20 — a paired sign test at p = 1.0. **A null is on record as a positive result, about the served model's covariate arm.**
- **Keys** `hansen_model_2011` (R27), `brigato_there_2025` + `hewamalage_look_2021` (R28), `hertel_explainable_2026` (R15 — attribution shares are not out-of-sample contribution), `haben_short_2019` (R14).
- **Code change** Small and the template already exists. `eval/weather_basis.py:217-274` and `eval/group_icl.py:302-356` already do exactly this correctly (per-fold loss vectors → `mcs.model_confidence_set` at 90% → paired moving-block bootstrap at a distinct seed). Port that pattern into the probe and widen the fold grid past `mcs.BLOCK_LEN = 7` — **at six folds the moving-block bootstrap is degenerate** (report 54: one admissible block, zero-width CIs, p-values pinned to 0.0/1.0). That defect already shipped a wrong "ships" verdict once; it must not ship a second.
- **Runtime** ~20–45 min (Chronos, `.venv-forecast`).
- **Expected outcome** The conclusion inverts from "covariates HELP" to "not separable from the univariate arm", which is **consistent** with S6's weather-marginal finding, with Hertel's 3.6%/2.7% attribution shares, and with Haben's temperature-often-detrimental result. Three independent lines agreeing is a Discussion paragraph.
- **Unblocks** W37; the exogenous-margin claim in Results; the R15 conformance row.

### R3 · Angelopoulos–Bates bound gated on the tie condition
- **Source** (d), closes **D-F1**.
- **Purpose** `eval/interval_calibration.py:309` computes the two-sided bound for every venue. `eval/interval_calibration.md:16` publishes `ellel: bound 0.9006` — on a venue that is 82% structural zeros, where `|residual|` ties at 0 and the **upper** limb's almost-surely-distinct-scores condition fails. The chapter states that condition in its own words.
- **Keys** `angelopoulos_conformal_2023`.
- **Code change** ~10 lines: compute the tie fraction in the calibration scores, and emit "not available at this venue (score ties)" instead of a number where it fails.
- **Runtime** minutes (regenerate `interval_calibration` artefacts).
- **What moves** No conclusion. The bound is load-bearing for nothing — which is precisely why leaving a checkable error in it is a pure loss.

### R4 · MASE-versus-RMSSE ordering invariance — **the evidence G1 should be decided on**
- **Source** (b) + (d), serves **D-D1** (defensible-divergence decision).
- **Purpose** G1 is currently a preference. This converts it into a measurement: does the served-model **ordering** change when the headline moves from MASE to RMSSE, at the two venues that admit a scaled error?
- **Keys** `kolassa_we_2023`, `hewamalage_forecast_2023`, `kolassa_why_2020`, `hansen_model_2011`.
- **Code change** Minimal. RMSSE is already computed on the same four bases as MASE, and is already the MCS **secondary** loss in `tab:mcs-config`; report 44 runs the MCS at both. The run is to report the MCS 90% set and the ranking under **each** loss side by side, per venue.
- **Runtime** ~10–30 min (reuses committed fold vectors where available).
- **Why it matters more than adopting RMSSE outright** If the ordering is invariant, keeping MASE costs nothing and the limitation is written with a measurement behind it. If the ordering moves, that is itself the strongest possible argument for switching, and it must be known before the Discussion is drafted.
- **Note** This does not pre-empt the human decision on D-D1. It supplies the evidence for it.

### R5 · TabPFN-TS as a ladder entrant — **human call, methodology change (gate 1)**
- **Source** (a), serves **D-D5**.
- **Purpose** `sec:rw-rhythm` argues that 3 venues / 1 year / few regressors sits inside the regime where **Chronos-2 and TabPFN-TS** are licensed. One was tested.
- **Keys** `hoo_tables_2026`, `hollmann_accurate_2025` (11M params, up to 10,000 samples — verified verbatim this session).
- **Code change** A new rung in `models/ladder.py` plus its dependency. Non-trivial.
- **Runtime** ~30–60 min plus setup.
- **The real cost, and why it is a gate** Adding an entrant after the ladder has run re-opens model selection. The project's pre-registration by commit ordering is called *"the strongest thing in the project"*; a late entrant needs its own pre-registered gate committed **before** the run, or it damages the asset it is meant to strengthen.
- **The alternative, free** Narrow the review sentence to name only Chronos-2. Costs one clause; costs the regime-fit argument some of its force.

### R6 · WLS_v unbiasedness check
- **Source** (d), closes **D-F7**.
- **Purpose** `wickramasuriya_optimal_2019` gives minimum variance **among unbiased** linear reconciliations. Nothing in the project states or tests that the base forecasts are unbiased, and the Croston-adopted nodes are exactly where a median-eliciting base is expected to be biased low.
- **Keys** `wickramasuriya_optimal_2019`, `kolassa_we_2023`.
- **Code change** ~20 lines in `hierarchy/reconcile.py`: report the mean signed residual per node on the held-out calibration block alongside `w`.
- **Runtime** minutes (regenerate A6).
- **Outcome either way is publishable** Unbiased ⇒ the precondition is met and stated. Biased ⇒ a named limitation on the reconciliation, which is more honest than the current silence.

### R7 · Staleness check on the `eval/chronos2_*.md` artefacts
- **Source** (c).
- **Purpose** Report 57's staleness sweep classified 15 artefacts. Three were **excluded for want of torch** (`eval/chronos2_*.md`). `.venv-forecast` has chronos 2.3.1 / torch 2.12.1, so the exclusion no longer holds. Report 57's own lesson — *"a stale artefact hides a broken generator"* — was cashed in once already when `weather_diagnostic.py` turned out to have been crashing since report 54.
- **Runtime** ~20–40 min (Chronos).
- **Note** R2 regenerates one of these three; this covers the rest. Fold R2 into it if run together.

### R8 · S8b cache build → S8c / ECE / temperature scaling / agent-vs-constants — **BLOCKED**
- **Source** (a) + (d), closes **D-U1**, **D-U7**, weaknesses **W4** (Fatal), **W10**, **W26**.
- **Purpose** The whole Objective 3 results section and Objective 4's calibration leg.
- **Keys** `guo_calibration_2017` (*"We use ECE as the primary empirical metric"*; temperature scaling *"often the most effective method"*), `fu_prism_2026`, `dixon_independence_2007`, `zheng_judging_2023`, `bavaresco_llms_2025`.
- **Correction to three project documents** The pipeline spec (§2 stage 10, *"cheap, unblocked"*), the state brief (§3 gap 3, *"Cheap, unblocked, and closes weaknesses 10 and 26"*) and the evidence pack (§7.1, *"none of them needs another party"*) all record ECE as **unblocked**. **It is not.** Verified this session: `eval/agent_calibration.py` is fully implemented and correct (M16 and M26 both CLOSED — Guo's `(lo, hi]` edges, 15 bins, `fit_temperature` per Guo §4.2), but `run()` calls `collect_records`, which needs `eval/agent_cache.json`. **That file does not exist**, and `ResponseCache` with `allow_live=False` raises `CacheMiss` on every scenario. Populating it requires `--build` with a key.
- **The upside of the same finding** S8b, S8c and ECE are **not three pieces of work — they are one command**. `eval/agent_calibration.py` already computes the cost-ratio sweep, ECE, Brier, the reliability diagram, temperature scaling and agent-versus-six-constants in a single `run(allow_live=True)`. Everything downstream of the key is built and tested.
- **Command when a key exists** `.venv-eval/bin/python -m eval.agent_calibration --build`
- **Runtime** ~644 injections, one call per scenario, temperature 0. Estimate **60–120 min** wall clock plus API cost; deterministic and cached thereafter, so it is a one-time spend.
- **Blocker** Anthropic API key (Ryan, ask 1; chase date 2026-08-04 has passed).

---

## Requires no run — transcription or prose only

Listed so they are not confused with experiments. All are gate 5 (Overleaf push).

| Item | Closes | Where the data already is |
|---|---|---|
| `tab:ladder` dispersion — sd, se, n per rung; drop the bolded winner | **D-F4**, W5, **W36** (the single named reason Distinction is "Not met") | `log/43_G17b_Fold_Count.md` §3 |
| Winkler table dashes → the computed means; coverage cells → Clopper–Pearson intervals | **D-F3** | `log/49:135`; `[0.899, 0.927]`, `[0.951, 0.973]` |
| `tab:bases` bootstrap intervals — the Ellel 65.6%-wide interval **is** the no-defensible-basis argument and is currently invisible | **D-F3**, W49 | `log/45:97-103` |
| MCS subsection: state B = 1000, block length 7, seed 93, the statistic, the elimination rule, and that the MCS **is** the multiple-comparison correction over nine rungs | **D-F4** | `log/44` §4a, pre-registered |
| State the Ask-F1 sweep's degeneracy as **our instance**, not only as a general condition | **D-F5**, W27 | the sweep itself |
| Threat model for the retrieval store | **D-F6** | `zou_poisonedrag_2025` |
| Re-cite Sun & Yu to **Corollary A.2**, and present observed-state Mondrian as **our** one-line extension | **V2**, D-D4 | this session's verification |
| State the p>2 constant-zero condition as the chapter's own derivation | **V1** | this session's verification |
| Chatfield: state that the remedy is a **cost** objective whose two parameters were never elicited | **D-D2** | evidence pack §2.3 |
| Fix `numbers_audit` MISMATCH 1–4 (the "fifth of nine" that is second; "reject" that is demote; the Ellel flip's cause; the `n/a` cell that scored) | W37-adjacent | `numbers_audit_resolutions.md` |

---

## Dependency shape

```
UNBLOCKED, today:        R1 → R2 → R7      (R2 and R7 share a venv and a warm cache)
                         R3, R4, R6         (independent, minutes each)
HUMAN CALL first:        R5                 (gate 1; needs a pre-registered gate committed before the run)
BLOCKED on Ryan's key:   R8 → the entire Objective 3 results section → contribution leg two → Conclusion
```

**The critical path still runs through one API key.** What changed today is the
size of what sits behind it: not three separate experiments but one command whose
downstream code is already written, tested and audited. And what came off the
critical path is nothing — ECE was never on the unblocked side, contrary to three
project documents.
