# apps/web — Next.js browser bundle env
#
# The API apps/web talks to. Production deployments must set this to the
# public URL of the NestJS API. The api's WEB_ORIGIN allowlist (apps/api/src/main.ts)
# must include this URL's origin for CORS to succeed.
NEXT_PUBLIC_API_URL=http://localhost:3001
