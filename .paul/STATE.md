# Project State

## Project Reference

See: .paul/PROJECT.md (updated 2026-04-13)

**Core value:** Hospitality staff and managers can get instant, accurate answers about stock, ordering, procedures, equipment, and contacts — like having a knowledgeable GM available 24/7 via chat.
**Current focus:** v0.2 Multi-Tenant WhatsApp — Phase 4 (Dynamic Document Intelligence) underway. Plan 04-01 (broadened extraction layer: XLSX/CSV/PPTX/image-via-Claude-vision) CREATED 2026-04-21 and awaiting approval. Phase 4 finalized at 5 plans during 04-01 scope-split. Classifier + taxonomy + procedural model + scheduler + WhatsApp runtime follow in 04-02→04-05.

## Current Position

Milestone: v0.2 Multi-Tenant WhatsApp (v0.2.0) — ~94% (Phase 1 + 2 + 3 engineering COMPLETE; Phase 4 IN PROGRESS at Plan 04-01 APPLY COMPLETE awaiting UAT + UNIFY)
Phase: 4 of 4 (Dynamic Document Intelligence) — Plan 04-01 (Broadened extraction layer) APPLY COMPLETE — all 3 tasks shipped (Task 1 `1b480b0` XLSX+CSV+sanitiseError, Task 2 `6826b4d` PPTX, Task 3 `52b90f6` image-via-Claude-vision+Prisma migration). SUMMARY written + staged. Structural AC Pass for AC-1/2/3/5/6/8; AC-4 Partial Pass with HEIC→D-04-01-J scope reduction; AC-7a/7b pending operator UAT.  ORIGINAL PLAN CONTEXT: 3 tasks, standard track, autonomous:true. Covers XLSX + CSV (via exceljs + csv-parse) + PPTX (library TBD via APPLY WebFetch) + image-via-Claude-vision (reuses Phase 3 Plan 03-03 multimodal infra + factored `sanitiseError` shared util). 8 acceptance criteria (AC-7 split into 7a ingestion-hard-gate + 7b retrieval-quality-observational). Enterprise audit performed 2026-04-21 10:25 — verdict: conditionally acceptable pre-fix → enterprise-ready post-fix. Applied 4 must-have (M1 extracted-text-never-in-logs boundary + grep-verify, M2 sanitiseError factored to apps/api/src/common/sanitise-error.ts from 03-05 + InfobipVerifyService switched to import, M3 canary+Demo-Org-manager UAT preconditions documented + runtime-assert, M4 AC-7 split to separate ingestion gate from multi-sheet embedding-dilution retrieval score) + 9 strongly-recommended (S1 tenant-scope boundary on image bytes both Option A/B, S2 MAX_CONCURRENT_IMAGE_EXTRACTS=3 semaphore with 15s queue timeout + docs.image_extract_queued log, S3 COMMITTED Option A + csv-parse/sync defaults at plan time [no APPLY-time A/B variability], S4 sourceImageMime column consistency across output+AC, S5 mime field on docs.uploaded log for per-format slicing, S6 mapApiError UI coverage confirmation for unsupported-mime/corrupt-bytes/timeout/empty-result, S7 multer 15MB ceiling pre-per-MIME-cap tradeoff acknowledged + D-04-01-H registered, S8 magic-byte-on-new-formats-only consistency decision documented + D-04-01-I registered, S9 image-bytes object-storage migration deferred D-04-01-F with concrete trigger). 5 deferred with explicit rationale (prompt-injection defense / magic-byte retrofit / locale-date / SonarQube / prod migration). Full audit at .paul/phases/04-dynamic-document-intelligence/04-01-AUDIT.md. Phase 4 finalized at 5 plans during scope-split: 04-01 extraction / 04-02 classifier+taxonomy+owner UI / 04-03 procedural model+schedule extraction / 04-04 scheduler+WhatsApp notifications / 04-05 WhatsApp runtime. Canary guardrail: 3 beerhall xlsx files in docs/ (git-untracked) are test inputs only — grep-verified zero code references.

**Phase 3 closure context (2026-04-21):** Plans 03-04 (Twilio→Infobip WhatsApp) + 03-05 (Twilio SMS OTP → Infobip 2FA) both shipped. Project is 100% Twilio-free: `env.twilio` removed from AuthEnv (compile-time proof), `.env.example` contains zero `TWILIO_*`. D-01-03-F explicitly closed. Phase 3 commits: `871c8ec` + `5f23bdc`. UAT pending: Infobip Portal runbooks for both. **Phase 4 pivot:** original "Coolify Deployment" removed from roadmap (user self-managing deployment) — replaced with "Dynamic Document Intelligence" per 2026-04-21 discussion. Motivation: plain RAG can't handle procedural artifacts requiring state tracking + execution, not retrieval; every company different, so the system must evolve its own taxonomy without dev releases. Vision: **AI as literal general manager** — proactive scheduling + notifications + completion tracking, not just reactive Q&A.
Plan: 03-02 (probe-whatsapp.ts + HMAC signer + ChatService test-mode envs) — COMPLETE. Loop closed. Commits: `2ed2e0c` plan+audit, `9935582` Task 1, `80f1c3c` Task 2, UNIFY commit pending. 2/2 tasks DONE + Qualify PASS. 18/18 assertions PASS in stub mode (W0 pre-cleanup + W1-W17 coverage of every 03-01 AC including the previously-missed dev-bypass positive path). Probe-whatsapp idempotent — second consecutive run also 18/18. Zero regressions on probe-api 61/61 + probe-auth 54/54. 4 deviations documented in SUMMARY: (1) Venue schema has `type` required not `city`; (2) DATABASE_URL heuristic broadened to include neon.tech (project DB convention per CLAUDE.md); (3) NestJS 2-arg Logger serializes payload to separate Object() line — assertions use event-name + DB count + log-count signals over payload-field grep; (4) W11 dev-bypass test must unset all 3 Twilio Verify env vars (not just TOKEN) because assertAuthEnv all-or-nothing check runs per-request. Scope adjustment: pre-cleanup promoted to counted W0 assertion to hit plan's N≥18 target. Real-Claude mode NOT run in this apply (costs ~$0.30-1.00); documented as pre-release checkpoint. Plan 03-01's D-03-01-F through D-03-01-K deferred items all unchanged. No new deferred items registered this plan. Next plan: 03-03 multimodal inbound (image via Claude vision) + typing indicator + proactive-suggestion opener.
Status: Plan 02-01 scopes the schema + service + retrieval changes needed to close the documented cross-org leak on `KnowledgeItem` rows with `venueId = NULL` before Phase 2 feature work (file upload) lands in 02-02. 3 tasks + 1 non-blocking post-deploy note (UAT replaced by probe A36/A37 per audit S2). `autonomous: true`. Files: schema.prisma + new migration + ingest.service + docs.service + retrieval.service + tool-dispatcher + chat.service + suggestions.service + seed.ts + probe-api.ts + @gm-ai/types. Probe assertion target raised 44 → ≥52 (A30 list leak, A31 getById 404, A32 retrieval leak, A33 invalid-orgId contract, A34 seed orphan integrity, A35 tool-schema shape, A36 positive list path, A37 positive retrieval path). Enterprise audit performed 2026-04-20 11:45. Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix. 5 must-have + 7 strongly-recommended upgrades applied; 5 deferred with explicit triggers.

Phase 1 closes at 3/3. Plan 01-03 shipped PhoneModule (NestJS) + /settings/phone UI (Next.js) + 13 new probe-auth assertions (54/54 green total; plan required ≥40). probe-api 44/44 — zero regressions. Twilio driver modes: live/console/disabled (disabled is a getter-based kill-switch). AC-1-9 + AC-12 + AC-13-16 Pass. AC-10 probe-deferred (plan-documented single-process limitation). AC-11 UAT deferred (UI-only human walk). Deviations: (1) mode is getter, not constructor-cached — kill-switch works without redeploy; (2) disabled-check precedes pending-match guard — outage vs abuse distinguishable; (3) SID regex accepts SK prefix (API Keys); (4) code schema accepts [A-Z0-9-]{6,12} to cover both live numeric and console PROBE-{6} formats; (5) maskPhone imported by UI only — controller response intentionally unmasked. Full deviation rationale + decisions in 01-03-SUMMARY.md.
Last activity: 2026-04-21 15:35 — Plan 04-01 APPLY COMPLETE. Task 2 (PPTX via officeparser@6.1.0) shipped as `6826b4d` — AST-walking for per-slide output (## Slide <n>: <title>/body/[notes]), shared ZIP header util factored, UI + allowlist + per-MIME cap extended. Task 3 (image-via-Claude-vision) shipped as `52b90f6` — image-extractor.ts with Anthropic SDK + lazy client singleton + MAX_CONCURRENT_IMAGE_EXTRACTS=3 semaphore + 15s queue timeout + estimateUsd() cost calculator ($3/$15 per MTok for Sonnet 4.6); KnowledgeItem.sourceImageBytes Bytes? + sourceImageMime String? additive migration 20260421150000_knowledge_item_image_source applied clean to NeonDB; image-magic-bytes.ts factored from whatsapp-media-download (HEIC added); IngestInput extended with sourceImageBytes/sourceImageMime passed through DocsService.create → IngestService.ingest with new Uint8Array(buf) coercion at Prisma boundary. 4 APPLY-time deviations documented in SUMMARY: (D1) HEIC dropped from AC-4 scope — Anthropic SDK media_type union is jpeg/png/webp/gif only, registered as D-04-01-J with sharp/heic-convert server-side scope + first-upload trigger; (D2) Prisma 7 Bytes/Buffer variance fix; (D3) hand-written migration avoids pre-existing DB drift bleed (ChatConversation FK + organizations.updatedAt DEFAULT — D-04-01-K); (D4) 8 pre-existing tsc errors catalogued (7 express TS7016 + 1 xlsx Buffer TS2345, all on Task 2 baseline 6826b4d) → D-04-01-K. Build clean: api 77 files (swc), web compiled OK. All audit grep checks green: citation density 20 across 5 extractors, M1 log-redaction zero extracted/base64/data in logger calls, M2 sanitiseError imported from shared util by image-extractor with zero duplicates, S2 MAX_CONCURRENT_IMAGE_EXTRACTS + docs.image_extract_queued + docs.image_extract_cost all present, S3 sourceImageBytes in schema.prisma, canary guardrail zero matches. SUMMARY at .paul/phases/04-dynamic-document-intelligence/04-01-SUMMARY.md has full per-AC table, 6 deviations, commit list, APPLY-time PPTX library + Anthropic pricing decisions, D-04-01-A through K registered with concrete triggers, AC-7b retrieval-measurement template + cost-log sample template to be filled post-operator-UAT. Operator UAT blocks the remaining 6 AC checkboxes: AC-7a canary ingestion hard gate, AC-7b retrieval similarity observational, AC-8 UI walk, AC-4 image smoke, AC-5 rejection paths, post-canary cleanup. PREVIOUSLY: 2026-04-21 11:00 — Plan 04-01 APPLY Task 1 COMPLETE (committed as `1b480b0`). PAUSED mid-APPLY per user at natural task boundary. Task 1 delivered: sanitiseError factored to apps/api/src/common/sanitise-error.ts (InfobipVerifyService consumes shared util, zero duplicate definitions, audit-M2 satisfied); XLSX extractor via exceljs + CSV extractor via csv-parse/sync, both magic-byte gated and respecting MAX_EXTRACT_CHARS cap; dispatcher extended with 2 new switch cases (existing PDF/DOCX/TXT/MD branches byte-identical per AC-6); UPLOAD_MAX_BYTES_BY_MIME map replaces single ceiling (audit-S7 tradeoff acknowledged); docs.controller.ts plumbs ExtractError.reason through 422 as details: { reason }; apps/web mapApiError branches on reason per audit-S6; file-picker + BINARY_UPLOAD_EXTS extended. Build clean on api (73 files, was 71) + web (all routes). All audit grep checks passing (citation density xlsx=7 csv=3, M1 log-redaction zero logger calls in extractors/, canary guardrail zero beerhall/checklist matches, zero duplicate sanitiseError definitions). Two deviations from plan documented in commit: (D1) package name is `api` not `@gm-ai/api` as plan listed — used correct filter; (D2) audit-S5 already satisfied — docs.uploaded log at docs.controller.ts:151 already emits mimeType field, no code change needed. Previously: 2026-04-21 10:25 — Plan 04-01 AUDITED (4M + 9S applied, 5 deferred); 2026-04-21 10:10 — Plan 04-01 CREATED. /paul:discuss 4 completed, CONTEXT.md committed as `5bdd888`. /paul:plan 04-01 scope-split: one combined classifier-and-extraction plan → two (04-01 extraction only, 04-02 classifier+taxonomy); phase finalized at 5 plans. Plan 04-01 has 3 tasks + 8 ACs covering XLSX/CSV/PPTX/image-via-Claude-vision extraction. Previously: 2026-04-21 09:25 — Phase 3 CLOSED. Plans 03-04 + 03-05 UNIFY complete, committed as `871c8ec` + `5f23bdc`. Phase 4 pivoted to "Dynamic Document Intelligence". 2026-04-20 10:58 — Plan 01-03 SUMMARY written with all 10 AC assessed. 10 must-have upgrades (M1 open-redirect guard via isSafeRedirect helper, M2 atomic sign-up + org creation via databaseHooks.user.create.after replacing client-side organization.create, M3 idempotent migration UPDATE with WHERE organization_id IS NULL, M4 prod-safe seed guard refusing NODE_ENV=production + default password, M5 API_ERROR_CODES append venue-not-found/invalid-redirect/payload-too-large/organization-slug-conflict, M6 withOrgScope type-safe split into withOrgScope for org-direct tables + withOrgScopeVia for join-scoped, M7 HTTP logger redaction contract for /api/auth/* bodies + Cookie/Authorization headers, M8 assertAuthEnv fail-fast startup ban on process.env.X! non-null assertions, M9 8 KB body cap on /api/auth/* NOT exempted, M10 explicit advanced.defaultCookieAttributes with secure=isProd). 17 strongly-recommended (S1 password min=12, S2 bcrypt 72-ceiling, S3 strong demo password default demo-CHANGE-me-before-prod-Xk7t9, S4 generateOrgSlug + OrgSlugSchema + collision retry, S5 probe-api A24 slug prefix probe-api-other-, S6 probe-auth cleanup pre+post, S7 security-headers middleware nosniff+DENY, S9 autocomplete attrs on forms, S10 middleware order comment block + first-log-field-order probe, S13 90-min spike time-box + concrete exit criteria, S14 rollback data-loss SQL header, S15 email-enumeration-safe invalid-credentials contract + probe P10, S16 trustedOrigins via env.webOrigin, S17 probe A29 body-redaction runtime assertion). 7 deferred with triggers: D1 account lockout (public deploy), D2 persistent audit log table (SOC2 Type II), D3 GDPR cascade (first RtBF request), D4 migration rollback CI test (pre-launch), D5 secret rotation (post-POC ops), D6 cross-subdomain cookie (Phase 4), D7 session fixation probe (post-POC). probe-api assertion count raised 36 → ≥42 (A23 unauth, A24 cross-org, A25 authed baseline, A26 staff role denied, A27 8KB cap 413, A28 nosniff+frameguard headers, A29 log body-redaction); probe-auth 0 → ≥11 (P1-P8 plus P9 redirect, P10 email-enum, P11 hook rollback). New files: apps/web/src/lib/safe-redirect.ts + server-session.ts, apps/api/src/modules/auth/assert-auth-env.ts + generate-org-slug.ts, apps/api/src/common/security-headers.middleware.ts. Total files_modified grew from 41 → 46.

Progress:
- v0.2 Multi-Tenant WhatsApp: [█████████░] ~93% (Phase 1+2+3 complete; Phase 4 plan 04-01 CREATED)
- Phase 1 (Auth + Organizations): [██████████] 100% ✓ (3/3 plans complete)
- Phase 2 (Document Ingest UI): [██████████] 100% ✓ (2/2 plans complete)
- Phase 3 (WhatsApp Integration): [██████████] 100% ✓ (03-04 + 03-05 Infobip migration complete; 03-01/02/03 SUPERSEDED)
- Phase 4 (Dynamic Document Intelligence): [██░░░░░░░░] ~20% (Plan 04-01 CREATED of 5 planned; 04-01→04-05 breakdown finalized)
- v0.1 POC: [██████████] 100% ✓ (archived in .paul/milestones/0.1.0-ROADMAP.md, tag v0.1.0)

## Loop Position

Current loop state:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Plan 04-01 LOOP CLOSED 2026-04-21 15:40 — SUMMARY at .paul/phases/04-dynamic-document-intelligence/04-01-SUMMARY.md]
```

Next: `/paul:plan 04-02` for Classifier + Taxonomy + Owner UI (per Phase 4 5-plan breakdown in CONTEXT.md). Operator UAT for 04-01 (AC-7a/7b/8/4/5 + post-canary cleanup) happens ANYTIME before v0.2 release — UAT findings append to 04-01-SUMMARY.md without reopening the loop.

## Accumulated Context

### Decisions
- Implementation order defined in PAUL.md (Section 12): scaffold → schema → embeddings → seeder → retrieval → chat → controller → UI
- POC scope excludes auth, multi-tenancy, BullMQ queues
- 5-phase roadmap: Foundation → Embeddings & Seeding → Retrieval → Chat Engine → Web Interface
- Enterprise audit on 01-01: Applied 4 strongly-recommended upgrades (.npmrc, CORS, Turborepo v2 format, frontmatter completeness). Deferred 4 (ESLint, CI/CD, Husky, Docker). Verdict: enterprise-ready.
- 2026-04-18: Plan 01-01 APPLY — Tailwind v4 CSS-first used instead of spec's tailwind.config.ts. Phase 1 | Impact: none, AC-3 satisfied via @tailwindcss/postcss + CSS import.
- 2026-04-18: Plan 01-01 APPLY — apps/api/tsconfig.json overrides base to CommonJS/Node10 (required for NestJS decorator compilation). Phase 1 | Impact: none, build passes.
- 2026-04-18: Plan 01-02 — Adopted Prisma 7 (driver-adapter + prisma.config.ts) rather than pinning to v6. User-chosen option B. Phase 1 | Impact: PAUL.md §4.2 datasource adapted (no `url`); all future DB access goes through PrismaPg adapter.
- 2026-04-18: Plan 01-02 — Driver chosen: @prisma/adapter-pg (not @prisma/adapter-neon) for environment portability. Phase 1 | Impact: NeonDB + local Docker pgvector both work without adapter swap.
- 2026-04-18: Plan 01-02 — No NestJS PrismaService for POC — direct `import { prisma } from '@gm-ai/database'`. Phase 1 | Impact: introduce PrismaService in Phase 2+ only if DI lifecycle benefits show up.
- 2026-04-18: Plan 02-01 — voyageai v0.2.x uses named export `VoyageAIClient` (not default import shown in PAUL.md §6). Phase 2 | Impact: any later plan copying from PAUL.md §6 must convert the import; flagged for audit.
- 2026-04-18: Plan 02-01 — No @nestjs/config for POC — dotenv at bootstrap suffices. Phase 2 | Impact: single-file migration to ConfigService if multi-env concerns surface later.
- 2026-04-18: Plan 02-01 — Probe scripts pattern — apps/api/scripts/*.ts run via tsx, exposed as `probe:*` npm scripts. Phase 2 | Impact: same pattern for future live-API verification (Claude, retrieval).
- 2026-04-18: Plan 02-02 — Workspace packages with runtime exports must compile to dist/ (not main: src/*.ts). Phase 2 | Impact: @gm-ai/database now has dist/; @gm-ai/types will need same treatment when Phase 4 consumes its Zod schemas at runtime.
- 2026-04-18: Plan 02-02 — Lazy Prisma singleton via Proxy in @gm-ai/database. Phase 2 | Impact: import order no longer couples to env loading; any context (CLI, test, serverless) can load env at any time.
- 2026-04-18: Plan 02-02 — CLI commands run via `nest build && node dist/src/...` (not tsx) because esbuild doesn't emit emitDecoratorMetadata. Phase 2 | Impact: all nest-commander CLIs rely on the compile step; swc makes it ~36ms.
- 2026-04-18: Plan 02-02 — Deterministic seed UUIDs (d0000000-*, e0000000-*). Phase 2 | Impact: Phase 3 retrieval test fixtures can hardcode IDs like Carlsberg Lager=d0000000-0000-4000-8000-000000000001.
- 2026-04-18: Post-02-02 — Switched NestJS compiler to swc (via @swc/core + .swcrc). Phase 2 | Impact: `nest build` down from ~5s → ~36ms; swc emits decoratorMetadata correctly so DI still works; dist/ now nests under dist/src/ (scripts updated).
- 2026-04-18: Post-02-02 — Model bumped from `claude-sonnet-4-20250514` (deprecates 2026-06-15) to `claude-sonnet-4-6`. Phase 2 | Impact: PROJECT.md constraint + EnrichmentService in sync; no model tooling plan needed pre-launch.
- 2026-04-18: Post-02-02 — Enrichment retry + markdown-fence strip — Claude 4.6 occasionally wraps JSON in ```json fences. Fence-stripper + 1-retry before fail-open. Phase 2 | Impact: all 6 SOPs enriched reliably; no silent empty aiTags.
- 2026-04-18: Post-02-02 — Shared env loader `apps/api/src/load-env.ts` — walks up from __dirname to find pnpm-workspace.yaml, loads sibling .env. Phase 2 | Impact: works across tsx + nest build + swc + any future runtime; no hardcoded `../../../.env` strings.
- 2026-04-18: Post-02-02 — Explicit `ssl: { rejectUnauthorized: true }` passed to PrismaPg, sslmode stripped from URL. Phase 2 | Impact: pg v8 sslmode deprecation warning silenced; future pg v9 upgrade is safe (won't trigger the alias semantics change).
- 2026-04-20 12:15: Enterprise audit performed on Plan 02-02. Verdict: conditionally acceptable → enterprise-ready after fixes. Applied 6 must-have (M1 @RequireRole on upload, M2 sanitizeUploadTitle, M3 reject empty extraction, M4 docs.uploaded audit log, M5 CORS DELETE method + probe-runner symmetry + MulterError→413 translation, M6 30s extraction timeout via Promise.race) and 6 strongly-recommended (S1 via M6, S2 A43 corrupt-file probe, S3 A38b/A38c real-binary probes, S4 hoist jsonDefault, S5 op field already specced, S6 A41 cascade verification). Deferred 5 (D1 magic-byte check, D2 rate limiting, D3 disk spooling, D4 FK survey closed in-audit, D5 .npmrc save-prefix). Probe assertion target raised 57 → ≥61. Plan status: ready for APPLY. | Phase 2 | Plan strengthened for enterprise / SOC-2 CC6.6 standards.
- 2026-04-20 12:32: Plan 02-02 APPLY ✓ + UNIFY ✓. All 3 tasks green; probe-api 52 → 61 (A38, A38b real PDF via unpdf, A38c real DOCX via mammoth, A39, A40, A41 with cascade verification, A42, A43, A44). probe-auth 54/54 unchanged. New files: apps/api/src/modules/docs/doc-extract.ts + multer-exception.filter.ts. Deviations: (1) Used existing Dialog instead of AlertDialog (no new deps per boundary); (2) multer-exception.filter.ts outside explicit files_modified but required for AC-1 contract; (3) tsconfig.types += "multer". Binary probe fixtures built at runtime via node:zlib.crc32 + hand-built PDF xref — no new deps, no checked-in binaries. | Phase 2 | Plan 02-02 closes the manager self-serve document CRUD surface end-to-end.
- 2026-04-18 12:58: Enterprise audit on 03-01. Applied 6 must-have (Decimal → double precision cast, vector input validation, venueId UUID validation, limit caps, deterministic secondary sort, removed spurious EmbeddingsModule coupling) + 6 strongly-recommended (RetrievalError type, structured debug logging without PII, latency assertion in probe, validation-error probe checks, typeof=='number' probe checks, HNSW index trigger condition). Deferred 4 (statement_timeout, persistent audit log → Phase 4 ChatMessage, unit tests, Zod schemas). Verdict: enterprise-ready post-fixes. Phase 3 | Impact: retrieval is a trust boundary for all downstream AI — hardening before APPLY avoids reworking Phase 4's consumer.
- 2026-04-18 13:10: **Plan 03-01 SUPERSEDED mid-APPLY (user diagnostic = Intent/C).** User flagged `RetrievalKind = 'sop' | 'stock'` as contradicting agentic-classification intent: "we should never have 'This document is X enum' — when AI indexes it, it figures out what kind of document it is … it adapts over time … if it has no answer, it relays that, or else you're hallucinating." Chose option [C] Full rethink from schema up (rejecting narrow [A] and moderate [B]). Phase 3 | Impact: schema + retrieval now both under review. Plan 01-02 Prisma schema (SopDocument vs StockItem split) is under question for the knowledge side; stock/ops-data side may stay structured. Next: /paul:discuss to rescope before any new planning.
- 2026-04-18 17:20: Enterprise audit on rewritten 03-01. Applied 4 must-have (autonomous=false + human approval checkpoint on destructive migration, `migrate dev --create-only` → review → apply flow, explicit upsert pattern replacing deleteMany-and-create wipe, sweep grep fixed with two-pass + `grep -v Mock` to avoid Mock*-prefix false positives, `toolCallLog` default `{}` → `[]`) + 4 strongly-recommended (concrete enrichment prompt shape + fail-soft log event, tiered probe thresholds with strict/soft/hard-fail, rollback procedure via `migrate resolve --rolled-back`, data-loss acknowledgement for ChatMessage columns). Deferred 5 (unit tests, prod-DB write-protect, metadata Zod schema, vector index, statement_timeout — all with explicit scope-owners or triggers). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 3 | Impact: destructive DB change now has audit-defensible change-management; seeder contradiction resolved before executor hits it; sweep step won't false-positive against refactored code.
- 2026-04-18 17:40: Plan 03-01 APPLY deviation — Prisma 7 `migrate dev --create-only` is interactive-only; piped stdin doesn't bypass TTY check. Workaround: `prisma migrate diff --from-config-datasource --to-schema --script -o <path>` to generate SQL offline, then `prisma migrate deploy` to apply. Produces the same DB state as migrate dev. Phase 3 | Impact: all future destructive-DDL plans in non-interactive contexts follow diff→deploy pattern (not dev→dev).
- 2026-04-18 17:40: Plan 03-01 APPLY deviation — `VenueContact.upsert` not possible (no `id` in fixture, no composite unique key in schema). Replaced with `findFirst({ venueId, name, role }) → update-or-create`. Phase 3 | Impact: if future plans retrieve venue contacts as first-class entities, add `@@unique([venueId, name, role])`; for now the findFirst pattern is idempotent and fine.
- 2026-04-18 17:40: Plan 03-01 — Agentic enrichment works end-to-end: Claude inferred 6 distinct freeform docTypes (troubleshooting guide, best practice guide, checklist, closing checklist, emergency procedure, ordering guide) from the same 6 SOP fixtures. Proves the "no enum" architecture before Plan 03-02 extends it. Phase 3 | Impact: Plan 03-02 inherits a working `enrichKnowledgeDoc` pattern + structured `enrichment.failsafe` log event.
- 2026-04-18 17:50: Enterprise audit on 03-02. Applied 4 must-have (explicit `max_tokens: 2048` on Claude ingest call, required try/finally cleanup in probe-ingest, post-deletion sweep grep for EnrichmentService/enrichKnowledgeDoc/enrichSop, simplified cross-ref resolution to single content-contains path with deterministic orderBy) + 4 strongly-recommended (agentic-emergence threshold in probe-seed: hard-fail <2 of 6 rows with emergent-keys/crossRefs, WARN <3 of 6; retry-once on fail-soft in probe-ingest to absorb Claude flakes; bootstrap IngestModule not AppModule in probe-ingest for narrower scope; deterministic `orderBy: createdAt: 'asc'` on cross-ref findFirst). Deferred 5 (unit tests, rate limiting, O(N) cross-ref perf, negative-path Zod test, cross-ref consistency sweep — all with explicit scope-owners or triggers). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 3 | Impact: agentic-emergence claim now enforceable via probe-seed gate; probe-ingest robust to single Claude flake; ingest prompt headroom protects fail-soft from token truncation.
- 2026-04-18 18:45: Plan 03-02 APPLY — @gm-ai/types required `typescript` devDep; initial `tsc` failed with `sh: tsc: command not found`. Installed via `pnpm install --filter @gm-ai/types`, build now clean. Phase 3 | Impact: any future workspace package that adds a `build: tsc` script must also declare typescript as devDep — root-workspace typescript is not on PATH inside `pnpm --filter <pkg> build`.
- 2026-04-18 18:45: Plan 03-02 APPLY — Agentic emergence exceeded expectations. All 6 seeded docs emitted 8–11 emergent metadata keys each (`contactNames`, `errorCodes`, `evacuationRoutes`, `cellarTempRange`, `gasPanelMinBar`, `drinkingUpTime`, `musterPoint`, `timeOfDay`, etc.); 5/6 produced ≥3 cross-refs that resolved via content-contains. Phase 3 | Impact: Plan 03-03 retrieval has dense, hospitality-specific signal to filter/rank against; proves the `.passthrough()` + free-form-prompt contract over cautious tag-only enrichment.
- 2026-04-18 18:45: Plan 03-02 APPLY — Ingest is now a service, not seeder-internal code. IngestService owns full knowledge-doc persistence (upsert + vector UPDATE in one transaction); seeder's knowledge loop dropped to 12 lines; stock loop untouched (ops data stays on direct path). Phase 3 | Impact: Plan 04-01 chat-tool-use and Plan 04-03 adaptation loop both import `IngestService` directly; no duplication of the ingest flow across callers.
- 2026-04-18 19:00: Enterprise audit on 03-03. Applied 3 must-have (wrap EmbeddingsService.embedText in try/catch → fail('error', ...), shared `guarded<T>()` wrapper on every MockOps method so Prisma/exception errors return fail('error', ...) instead of throwing, deterministic `id ASC` secondary sort on both retrieval SQL variants) + 7 strongly-recommended (UUID regex validation on venueId in retrieval + mock-ops with fail-fast before DB, query length cap at 2048 chars with `retrieval.query_truncated` warn log, non-optional PII-safe structured log `retrieval.call` with queryHash instead of content slice, name ASC tiebreak on getStockBelowPar + id ASC on all Prisma orderBy, supplierNotes added to getUpcomingCutoffs response so chat can verbalize real cutoff text from seed, deterministic probe assertion for getStockBelowPar `>= 5` items [fixture has 11], two new probe checks: venueId-scoped retrieval branch + invalid-venueId fail-fast). Deferred 7 (unit tests, Zod runtime validation in probe, HNSW index, statement_timeout, rate limiting, real cutoff parsing from supplier notes, similarity range doc comment — all with explicit triggers or scope-owners). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 3 | Impact: ToolResult<T> contract now actually holds under fault modes; retrieval has audit-defensible call log without PII; probe's 9 assertions catch regressions the prior 7 shape-only checks would've hidden.
- 2026-04-18 19:55: Plan 04-01 APPLY deviation — Zod 4's `.string().uuid()` strict-validates UUID version bits; seed UUIDs like `a1000000-0000-0000-0000-000000000001` have `0` in the version nibble and fail strict validation. Switched `TOOL_INPUT_SCHEMAS` (chat-tools.ts) and `SendMessageInputSchema` (chat.service.ts) to loose `z.string().regex(UUID_RE, ...)` matching the existing retrieval + mock-ops pattern. Phase 4 | Impact: tool schemas now consistent with retrieval/mock-ops trust boundary; any future @gm-ai/types consumer must use the same loose UUID regex, not `z.string().uuid()`.
- 2026-04-18 19:55: Plan 04-01 APPLY deviation — Added `zod: latest` as a direct dep of `apps/api/package.json` because pnpm's hoisting model does not expose `@gm-ai/types`'s transitive `zod` at runtime from the api package. Phase 4 | Impact: any future workspace consumer that imports `z` directly (not through a schema re-exported from @gm-ai/types) must declare zod as a direct dep.
- 2026-04-18 19:55: Plan 04-01 APPLY — ChatModule exports `ToolDispatcher` in addition to `ChatService` so Plan 04-02 (proactive suggestions) can reuse the dispatcher without re-instantiating. Phase 4 | Impact: 04-02 composes cleanly by importing `ChatModule` and injecting `ToolDispatcher`; no duplicated tool-routing logic.
- 2026-04-18 19:35: Enterprise audit on 04-01. Applied 6 must-have (per-call venue-context injection into system prompt so Claude knows venueId without asking [probe c2 would hard-fail without this], try/catch around the tool-use loop with `chat.anthropic_error` log + fallback assistant text so no orphan user messages, non-tool-use stop_reason empty-text handling with `chat.empty_assistant_text` log + default message, probe c3 credential-shape negative regex removed [false-positived on innocuous replies containing 6+ char words after "password"], Zod `SendMessageInputSchema` at sendMessage entry [UUID venueId, 1..8000 char userMessage, UUID conversationId], deterministic history orderBy tiebreak `[{ createdAt: 'asc' }, { id: 'asc' }]`) + 4 strongly-recommended (conversationId existence + venueId-ownership preflight to prevent cross-tenant message injection, per-round `chat.claude_call` observability log [round, stop_reason, input_tokens, output_tokens, latency_ms] for Plan 04-03 eval harness, ToolDispatcher `tool_dispatch.error` structured log on exception, probe DB re-read assertions for c1/c2/c3 verifying persisted toolCallLog + retrievedItemIds match returned values). Deferred 7 (idempotency key, rate limiting, prompt-injection hardening, conversation summarization, streaming, per-turn cost budget, probe per-conversation timeout — all with controller-layer or telemetry-trigger owners). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 4 | Impact: probe c2 (below-par stock at the Crown) now has a viable path [pre-fix, Claude would ask for venueId and fail the assertion]; every user turn has a paired assistant outcome even on Anthropic 5xx/rate-limit [audit-defensible]; 04-03 adaptation loop has the per-round cost+latency signal baked in from day one.
- 2026-04-18 21:30: Enterprise audit on 04-03. Applied 6 must-have (MAX_RETAG_ATTEMPTS=3 failed-item lockout preventing thrashing on chronic-failure items; sourceMessageId → ChatMessage FK onDelete:SetNull + named `@relation("sourceMessage")` to disambiguate dual ChatMessage back-relations + @@index([sourceMessageId]) — prevents orphan queue rows; DRAIN_SOFT_DEADLINE_MS=60000 wall-clock ceiling on processReTagQueue with in-flight revert to 'queued' + adaptation.drain_deadline_reached log; captureRetrievalOutcome defensive type guards on toolCallLog shape + adaptation.retrieval_outcome_shape_unknown warn log — closes silent contract-drift failure; migration command fixes [removed spurious self-diff sanity parse, added mkdir -p for destination dir, added migrate resolve --rolled-back rollback procedure mirroring 03-01]; MAX_ENQUEUE_PER_FEEDBACK=10 cap on retrievedItemIds sliced before enqueue + adaptation.feedback_enqueue_capped log) + 7 strongly-recommended (adaptation.drain_summary log with remainingQueued count via prisma.reTagQueueItem.count; adaptation.capture_error payload spec { assistantMessageId, error: String(err).slice(0,200) }; probe-adaptation assertion 13 — concurrent Promise.all captureFeedback dedupe race test; kind-transition semantics documented — 'up' → 'down'/'regenerate' triggers enqueue + isKindTransition log field; probe-eval low-similarity side-effect soft assertion counting retag_queue_items with sourceMessageId IN capturedAssistantMessageIds; probe-adaptation assertion 14 — pre-insert failed+attempts=3 row, assert enqueueReTag returns {exhausted:true}; probe-adaptation assertion 15 — malformed toolCallLog shape defense). Deferred 7 (unit tests, BullMQ/scheduled drainer → Plan 05+ or post-POC, metadata-diff signal → waits on eval data, per-venue partitioning, soft-fail eval retry → CI-gated trigger, OTel metrics → cross-service plan, HTTP rate limiting → Plan 05-01 throttler). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 4 | Impact: ReTagQueueItem now has explicit cost ceilings + FK integrity + observable contract-drift; probe assertion count raised from 12 to 15; migration command as-written would have failed on first execution (missing mkdir -p) — fixed before APPLY.
- 2026-04-20 20:45: Enterprise audit on 03-04 (Twilio→Infobip WhatsApp provider migration — first post-supersession plan). Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix. Applied 7 must-have (M1 Express Request.rawBody TypeScript module augmentation at apps/api/src/types/express-augment.d.ts — implicit-any silent-failure risk closed if future middleware refactor touches rawBody; M2 INFOBIP_WEBHOOK_SECRET ≥32 chars min-length validation matching BETTER_AUTH_SECRET rigor — prevents weak-key foot-gun; M3 hex/base64 format regex reject BEFORE Buffer.from + timingSafeEqual — no control-flow relies on length-check catching malformed encoding; M4 HALT-CONDITION for APPLY-time scheme drift — if WebFetch reveals Infobip scheme materially differs from HMAC-SHA256-over-raw-body executor STOPS + raises checkpoint rather than guess-and-ship; M5 contact name HARD-DROP from logs — no waIdHash rename to contactNameHash, no hashed form, no raw; contact names are low-entropy with near-zero forensic utility; M6 middleware-order contract + boot-time Express-stack assertion in main.ts with process.exit(1) if /webhooks/infobip/whatsapp branch missing — prevents silent 403-on-everything if future middleware reordering; M7 D-03-04-G registered for INFOBIP_DRIVER_OVERRIDE=console default removal as pre-Phase-4 go-live blocker mirroring D-01-03-F pattern for SMS OTP). Applied 9 strongly-recommended (S1 raw() parser type allowlist ['application/json','application/*+json'] for Content-Type parameter tolerance; S2 signatureValidated:true flag in whatsapp.payload_invalid log distinguishes signed-but-malformed from unsigned-garbage for compliance reconstruction; S3 Infobip media-URL auth trial matrix — App header → 401 retry no-auth → 401 HALT+checkpoint, AC-12 enforces; S4 BATCH_DEADLINE_MS=12_000 soft deadline on controller for-loop with whatsapp.batch_deadline_reached log + per-skip debug — prevents slow batch starving Infobip retry window, AC-13 enforces; S5 INFOBIP_BASE_URL regex accepts apex (https://api.infobip.com) + subdomain forms — tight guess would reject standard-tier accounts; S6 D-03-04-H registered for ALLOW_WEBHOOK_DEV_BYPASS branch removal post-UAT — retained for manual curl testing only during Infobip Portal UAT; S7 D-03-04-I registered for orphan __resetForTest exports v0.3 review; S8 enumerated 12-step Infobip Portal UAT runbook in <output> — set webhook URL, join trial sender, send text, observe log events, send image, send audio, verify AC-completion greps; S9 // Source:<URL>·verified<date> citation discipline on every WebFetch-derived fact in source files). 5 deferred with explicit triggers: D1 HTTP rate limiting → public-deploy; D2 Infobip outbound retry semantics → first 5xx cluster; D3 outbound idempotency keys → first duplicate-message incident; D4 guard+adapter unit tests → project-pattern deferral; D5 parallel batch processing → if Infobip batching empirically bottlenecks. AC count raised 8 → 13 (AC-9 rawBody type-safe, AC-10 contact-name drop, AC-11 middleware contract + boot-assert, AC-12 media-URL auth trial matrix, AC-13 batch deadline). New file added to files_modified: apps/api/src/types/express-augment.d.ts. Task count unchanged (3). 4 deferred items registered on this plan (D-03-04-F/G/H/I) carry forward to Phase 4 go-live checklist alongside D-01-02-F/D-01-03-F/D-03-01-F. Full audit report at .paul/phases/03-whatsapp-integration/03-04-AUDIT.md. Plan ready for APPLY. Phase 3 | Impact: provider migration proceeds on audit-defensible footing; user's explicit "no probe" directive is respected (03-04 deletes probe-whatsapp.ts + probe:whatsapp script) while silent-failure modes closed by M1/M3/M4/M6; deferred items accumulate as Phase 4 exit-gate list; UAT runbook makes Infobip Portal cutover reproducible without undocumented tribal knowledge.
- 2026-04-18 22:00: Enterprise audit on 05-02. Applied 5 must-have (XSS posture — plain-text rendering with `whitespace-pre-wrap`, grep-enforced absence of `dangerouslySetInnerHTML` + markdown libs [Claude outputs are untrusted enough that unsanitized rendering ships prompt-injection XSS]; Suspense boundary wrapping `useSearchParams` in chat/page.tsx [Next 16 static-gen bails without it]; `app/chat/error.tsx` App Router boundary with reset button [uncaught component error = blank page]; X-Request-Id propagation in apiFetch — UUID per call, server echo, exposed via `ApiError.requestId` [closes the 05-01 observability loop]; accessibility baseline AC-9 — aria-label on icon-only feedback buttons, `role="log"`+`aria-live="polite"` on thread, icon+text severity not color-only, semantic article/section/label wrappers [WCAG AA blockers]) + 9 strongly-recommended (loading skeleton for conversation fetch via ChatThread `isLoadingHistory` prop + `app/chat/loading.tsx`, mutation cancel on venue switch via `queryClient.cancelQueries` + local state reset, Sonner `visibleToasts={3}` cap to prevent toast-stack pile-up on downed API, `suppressHydrationWarning` on root `<html>` for sonner/next-themes peer, Tailwind v4 + shadcn CSS-vars source URL pinned [https://ui.shadcn.com/docs/tailwind-v4] + explicit `@theme inline` requirement to prevent silent v3-vars-against-v4 drift, AbortSignal passthrough in apiFetch for React Query cancel-on-unmount, ApiError captures requestId for support/debug display, composer textarea fixed-height `rows={3}`/`min-h-[80px]` no auto-grow, optimistic bubble rollback + text restore on send error so user can retry without retyping). Deferred 8 (CSP headers → Coolify deploy plan; sanitized markdown renderer → separate plan with tag allowlist; virtualization → >100 messages trigger; Playwright/Vitest → project-pattern deferral; dark mode/i18n/Sentry/service-worker → post-POC; refetchOnWindowFocus → multi-tab trigger; idempotency keys → post-auth; server-response Zod validation → contract cost/benefit at POC scope). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 5 | Impact: UI-side observability loop now closes back to 05-01's requestId middleware; XSS threat model explicitly documented before APPLY (plan default would have drifted to dangerouslySetInnerHTML or unsanitized markdown); WCAG AA baseline embedded in AC-9 with grep-verification; App Router error/loading conventions established for 05-03 to follow.
- 2026-04-18 21:30: Enterprise audit on 05-01. Applied 9 must-have (CORS origin allowlist via WEB_ORIGIN env + credentials:true + methods allowlist [open CORS on Claude-spending endpoint = token-theft risk], GET /chat/conversations/:id tenant-scoped via `?venueId` query returning 404-not-403 on mismatch to avoid enumeration leak, explicit 32kb body-parser limit via `app.use(json({limit:'32kb'}))` before controllers [default 100kb = DOS window], canonical `ApiErrorResponse = {error:ApiErrorCode; details?:unknown}` with closed `API_ERROR_CODES` set in @gm-ai/types/api.ts [prevents 05-02 per-endpoint error handling drift], `userMessageField = z.string().trim().min(1).max(8000)` shared refinement rejecting whitespace-only input before Claude spend, AC-4 extended for valid-UUID-but-nonexistent venueId → 404 venue-not-found + probe A9c, `translate-chat-error.ts` single-source-of-truth helper consolidating inline regex substring matches from ChatController + grep-verification that no other file contains the regexes, probe-api pre/post cleanup by `channel='probe-api'` marker with FK-safe delete chain [prevents DB bloat that bit Plan 04-03 APPLY], cross-tenant GET probe coverage via A8b/A8c/A8d [random-UUID + Anchor-conv-Crown-query + missing query]) + 7 strongly-recommended (X-Request-Id middleware generating UUID on missing + echoing inbound, `http.request` JSON logger with PII rule [never log body/query/param VALUES — field names OK], Anthropic transient-failure `sendWithRetry()` once on 429/5xx mirroring 03-02 pattern, `app.enableShutdownHooks()` + SIGINT/SIGTERM handlers in probe so port :3099 frees on CTRL-C, Claude-cost banner at probe start ["~3 calls per run, ~$0.01–0.03"], AC-6 invalid-input branch downgraded to defence-in-depth comment, `zodPipe(schema)` factory replacing `new ZodValidationPipe(...)` inline). Deferred 9 (HTTP rate limiting → re-deferred with trigger "public-facing deployment OR probe >1 req/s sustained"; streaming/SSE → post-POC; OpenAPI → re-evaluate if 05-02 needs live specs; unit tests → project pattern deferral; OTel metrics → cross-service plan; global exception filter → unjustified at 5 endpoints; idempotency keys → UI double-submit guard in 05-02; degraded-state flag in SendChatMessageResponse → requires ChatService change, raise as 05-02 UX item; statement_timeout/HNSW → unchanged from Phase 3). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 5 | Impact: probe assertion count raised from 16 → 22; main.ts now a boundary-change file (previously would have been invisible); `ApiErrorResponse` establishes the contract Plan 05-02 consumes for ALL error-state UI. CORS allowlist is the only browser-side budget protection until auth ships — named deferral trigger documented in boundaries SCOPE LIMITS.
- 2026-04-18 20:25: Enterprise audit on 04-02. Applied 6 must-have (CROWN_VENUE_ID factual error — Task 3 hardcoded `b1000000-…-1` which is SUPPLIER_MATTHEW_CLARK; fixed to import `VENUE_CROWN` from seed-data.ts [probe would have hard-failed on first run]; conversationId cross-tenant preflight in onTurn via `prisma.chatConversation.findUnique` with venueId-match check + `suggestions.conversation_mismatch` warn log — prevents log correlating another tenant's conversationId with this venue's suggestion output; 3000ms per-dispatch timeout via `runDispatchWithTimeout` helper [onConversationOpen runs at session load; unbounded dispatch blocks UI]; `suggestions.both_tools_errored` error-level escalation log when all invoked branches fail so ops can distinguish "no data" from "service degraded"; explicit PII rule — userMessage NEVER logged in any form, grep-verified; `sanitizeText` helper strips control chars + truncates at word boundary before return) + 5 strongly-recommended (single-call `generatedAt` invariant so a batch's suggestions all share one timestamp; WARN-severity probe assertion — seeded Neck Oil IPA with currentQty=0 now exercises the warn path end-to-end; dedupe probe assertion firing both STOCK_GATE + CUTOFF_GATE; seed-data import pattern for VENUE_CROWN + VENUE_ANCHOR; `composeSuggestions` pure helper shared between onConversationOpen + onTurn to prevent drift). Deferred 7 (rate limiting → Plan 05-01 throttler guard; UI pagination → Plan 05-01; heuristic false-positive tuning → Plan 04-03 adaptation loop; OTel/metric emission → cross-service telemetry plan; unit tests on helpers; idempotency → deterministic by construction; cost budget → N/A without Claude). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 4 | Impact: probe would have failed on first run without the UUID fix; log-integrity posture now matches Plan 03-03's retrieval PII stance; operational failure modes (timeout + double-error) now have distinct observable signals; `runDispatchWithTimeout` establishes a pattern Plan 04-03 + 05-01 should adopt for any non-Claude dispatcher consumer.
- 2026-04-18 22:30: Plan 05-02 UNIFY — mapApiError hoisted to `apps/web/src/lib/map-api-error.ts` (plan Task 2 step 12 permitted "inline in page or in lib"; chose lib). Phase 5 | Impact: both `useFeedback` hook + chat `page.tsx` import one source; establishes client-side single-source error-translation pattern mirroring 05-01's `translateChatServiceError`. Any future client-side server-error → user-string mapping belongs in `apps/web/src/lib/map-*-error.ts`, not duplicated at call sites.
- 2026-04-18 22:30: Plan 05-02 UNIFY — APPLY executed cleanly with zero auto-fixes. 26 created + 4 modified files. AC-1 through AC-10 all Pass; UAT (AC-7, 14-step human walk) approved; `pnpm --filter api probe:api` regression 29/29 green alongside running web app. Phase 5 | Impact: Patterns shipped for 05-03 to inherit — apiFetch singleton with X-Request-Id capture, URL-as-state, zero-DOM empty states, toast-as-error-surface, App Router error.tsx/loading.tsx per segment, icon+text severity (never color-only), aria-label + aria-pressed on icon-only buttons, role="log"+aria-live="polite" on any new list surface.
- 2026-04-19 09:30: Plan 05-03 UNIFY — APPLY executed cleanly with zero auto-fixes. 19 created + 6 modified files (~1,336 LOC). AC-1 through AC-9 all Pass; UAT (AC-9, 19-step operator walk) approved; `pnpm --filter api probe:api` 36/36 green (D1–D7 added); `pnpm --filter web build` clean. Two helper-factoring clarifications documented in SUMMARY: (1) `debug.access` emitted via shared `logAccess()` helper (1 literal grep hit in debug.service.ts:30) rather than 3 inline emissions — plan verify step 12 expected ≥3 hits but DRY factoring is functionally stronger (all 3 endpoints still emit the event via helper); (2) `RETENTION_90D_MS` applied via `retentionCutoff()` helper (2 literal grep hits: import + helper def) rather than 3 inline `new Date(Date.now() - RETENTION_90D_MS)` sites — same factoring principle. Phase 5 | Impact: patterns shipped for post-POC observability work — `DebugRequestIdBadge` + `apiFetchWithMeta<T>()` as operator handle, dual-layer noindex + amber banner as pre-auth defence-in-depth, `RETENTION_90D_MS` typed constant as retention-config template, `debug.access` per-call structured log as operator/user traffic separator without modifying shared http logger. Future audits should grep for helper call sites (`logAccess(`, `retentionCutoff(`) not literal constants.
- 2026-04-18 23:05: Enterprise audit on 05-03. Applied 6 must-have (cross-tenant retag-queue leak — original `{ knowledgeItem: { OR: [{ venueId }, { venueId: null }] } }` exposed other tenants' state drift on shared globals [SOC-2 CC6.6 failure]; replaced with strict `OR: [{ sourceMessage: { conversation: { venueId } } }, { sourceMessageId: null, knowledgeItem: { venueId } }]`, probe D5 guards; explicit `refetchOnWindowFocus: false, refetchOnReconnect: false` on both debug hooks + manual Refresh button in page header wired to `queryClient.invalidateQueries(['debug'])` — prevents unbounded refetch storms when operator tab-switches; server-side 2048-char truncation on `toolCallLog.result.data[].content` with `__truncated: true` marker + client-side 64KB JSON viewer cap — prevents 100KB toolCallLog entries from freezing main thread, probe D6 guards; `X-Robots-Tag: noindex, nofollow` via apps/web/next.config.ts headers() + `robots: { index: false, follow: false }` metadata in new apps/web/src/app/debug/layout.tsx + amber warning banner — defence-in-depth for pre-auth debug URL (NOT replacement for auth), probe D7 guards; explicit `mapStatusCount()` function in apps/api/src/modules/debug/truncate.ts initializing all 5 known buckets + rolling unknowns into `failed` with `debug.unknown_status` warn log — replaces unreachable comment-only bucketing; removed dead `tabs.tsx` + @radix-ui/react-tabs dep from files_modified — two-pane layout is a simple grid) + 9 strongly-recommended (truncateAtWord/truncateToolCallLogEntry/mapStatusCount moved from @gm-ai/types to apps/api/src/modules/debug/truncate.ts — @gm-ai/types stays pure types; 90-day retention window on conversation + message debug queries via typed `RETENTION_90D_MS` constant — bounds GDPR subject-access scope; `debug.access` structured log event per call with `{ requestId, path, venueId, resource, outcome, latencyMs }` — separates operator-debug from user traffic without modifying 05-01's http logger; `/debug/error.tsx` static-only [no useSearchParams / useQuery / apiFetch] — grep-enforced; probe D5/D6 seed rows keyed to `channel='probe-api'` for hermetic + cleanup-safe runs; service-layer `safeLimit` clamp defense-in-depth even with zodPipe; probe banner adds "Debug cost: +7 DB queries per run, no additional AI calls" matching 05-01 cost-transparency; new `DebugRequestIdBadge` in page header + `apiFetchWithMeta<T>` variant surfacing X-Request-Id on SUCCESS path for operator log correlation; assertion count correction 29 → ≥36). Deferred 2 (radix `"latest"` pinning → post-POC tooling plan; log aggregation UI → cross-service OTel plan). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 5 | Impact: probe assertion count raised 29 → ≥36 including tenant-isolation + JSON-truncation + noindex-header guards; cross-tenant leak on shared globals closed before APPLY; `RETENTION_90D_MS` establishes retention-config pattern; `DebugRequestIdBadge` + `apiFetchWithMeta` set the operational-handle pattern post-POC observability work should inherit.
- 2026-04-19 18:00: Enterprise audit on 01-02 (Invitations — second v0.2 plan, consumes 01-01 trust boundary). Applied 10 must-have (M1 AbortSignal.timeout 5000ms on Resend fetch — prevents indefinite hang; M2 emailVerified gate on accept with dev bypass + explicit WARN log — closes unverified-signup account-hijack vector; M3 buildSubject() CRLF strip — prevents email-header-injection via malicious organization name; M4 InviteRole narrowed to ['manager','staff'] — owner NOT invitable, prevents back-door owner creation, new 400 invalid-invitation-role; M5 optimistic-lock updateMany WHERE status='pending' on accept — closes double-accept race under concurrent load, probe P16b; M6 expired response matrix pinned ALWAYS 410, revoked ALWAYS 404 — idempotent across retries; M7 MAX_PENDING_INVITATIONS_PER_ORG=50 cap + lazy-GC expireStaleInvitations, 429 invitation-limit-reached — prevents mass-email-bombing vector; M8 Session.activeOrganizationId update scoped to specific sessionId from request context — does not silently switch active-org on other devices; M9 new error code already-a-member 409 — distinct from sign-up context email-already-registered; M10 MAIL_DRIVER_OVERRIDE='console' forced in probe-auth — prevents accidental Resend invoice during probe runs). 8 strongly-recommended (S1 list pagination limit/offset/total/hasMore server-clamped to [1,100]; S2 in-memory per-IP 60/min throttler on unauth preview endpoint; S3 probe slug prefix probe-invites- isolated from 01-01's probe-auth- to prevent cleanup-glob collision; S4 structured log invitation.accepted { invitationId, acceptorUserId, organizationId, role, acceptorEmailHash:sha256.slice(0,16) } at info level post-transaction — audit-defensible access-grant event; S5 invitation.revoked structured log; S6 client-side terminal-vs-transient error classification + React 19 strict-mode useEffect guard — no infinite-retry on 410 expired; S7 sha256-prefix upgraded 8→16 for stronger audit forensics; S8 reissued:true flag in CreateInvitationResponse). 7 deferred with explicit triggers (D-01-02-A per-email-target rate limit → abuse report OR >5 orgs invite same email <1h; D-01-02-B orphan invitation GC → >1000 stale rows OR GDPR RtBF; D-01-02-C Resend retry+DLQ → p95 success <95%/7d; D-01-02-D bounce webhook → Resend integration deepens; D-01-02-E reminder UI → post-POC review; **D-01-02-F actual email-verification flow removing dev bypass → pre-public-deploy BLOCKER** — carry to Phase 4 Coolify go-live checklist; D-01-02-G Redis-backed preview throttler → multi-instance scale). probe-auth target raised ≥17 → ≥24 assertions (added P13b owner-invite-reject + P16b concurrent-accept race + P17b pagination + P18 mail-driver-override + P19 invite-cap-429 + P20 already-a-member + P21 email-not-verified gate). API_ERROR_CODES tuple grew by 9 entries (original 5 + audit-added invalid-invitation-role/invitation-limit-reached/already-a-member/email-not-verified). 2 new AC (AC-11 per-org cap + AC-12 pagination contract). Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix. Phase 1 | Impact: invitation trust boundary now has three independent defence layers matching 01-01 (compile-time InviteRole narrowing; runtime probe P13b/P16b/P18-P21; boot-time MAIL_FROM format validation + MAIL_DRIVER_OVERRIDE probe-time forcing). Patterns established for Plan 01-03 (phone-linking) to inherit: SMS-driver override pattern mirroring MAIL_DRIVER_OVERRIDE for probe-time Twilio stubbing; 2-layer verification (email in 01-02, phone in 01-03).
- 2026-04-20 10:58: Plan 01-03 UNIFY — APPLY executed cleanly with one in-flight correction (controller verify-path ordering: disabled-check before pending-match guard; probe P30 caught this on first run). 8 created + 9 modified files. All 16 AC assessed: AC-1-9 + AC-12 + AC-13-16 Pass (runtime-verified via probe-auth 54/54); AC-10 probe-deferred per plan (single-process script can't race two requests cleanly; Prisma @unique + P2002 catch is the defence); AC-11 UAT BLOCKER (UI-only walk — requires human at /settings/phone). probe-api regression 44/44 — no regressions. Key deviations documented: (1) TwilioVerifyService.mode as getter not constructor-cached field — kill-switch works without redeploy, P30 probe can flip TWILIO_DRIVER_OVERRIDE mid-run; (2) verify-endpoint disabled-check precedes pending-match guard — outage (503) vs abuse (400) are operationally distinct; (3) SID regex `/^(AC|SK)[A-Za-z0-9]{32}$/` accepts both Twilio Account SIDs and API Keys (SK prefix is the recommended service-integration credential); (4) code regex `/^[A-Z0-9-]{6,12}$/` covers both live 6-digit numeric and console PROBE-{6digits} formats in one Zod pipe; (5) maskPhone imported by UI components only (phone-status-card + phone-link-form) — controller intentionally returns unmasked phone so users see their own linked number. Patterns established for Phase 3 (WhatsApp): inbound From-lookup via `user.findFirst({ phoneNumber, phoneVerifiedAt: { not: null } })`; same driver-mode getter pattern for Twilio WhatsApp sender kill-switch; in-memory rate-limit Map pattern can be extended or swapped for Redis at multi-instance scale. Phase 1 | Impact: trust-boundary stack complete (auth 01-01 → invitations 01-02 → phone 01-03); Phase 2 (Document Ingest UI) unblocked. Pre-Phase-4 go-live blocker carry-forward remains: D-01-02-F email-verification flow removing dev bypass, plus new D-01-03-F (remove TWILIO_DRIVER_OVERRIDE=console from prod env before go-live — current default in .env.example is probe-safe but production-unsafe).
- 2026-04-20 11:45: Enterprise audit on 02-01 (KnowledgeItem organizationId — first v0.2 Phase 2 plan, closes documented SOC-2 CC6.6 cross-org leak on global KnowledgeItem rows with venueId=NULL). Applied 5 must-have (M1 migration prod-safety DO-block: `RAISE EXCEPTION` if orphan rows remain + demo-org missing, converts silent `SET NOT NULL` failure into operator-readable abort; M2 table-name case sanity step — explicit `\dt` check before writing migration, documenting `"Venue"` capitalised vs `"organizations"` lowercase vs `"knowledge_items"` snake_case; M3 tool-input schema hardening — `TOOL_INPUT_SCHEMAS.find_knowledge` MUST NOT accept orgId field, orgId comes ONLY from DispatchContext.ctx.orgId, inline comment + grep-verify + new probe A35; M4 retrieval invalid-orgId contract probe A33 — guards UUID validation from silent removal; M5 docs.cross_org_denied warn-level structured log emitted when getById sees a cross-org row + matching suggestions.org_mismatch log on suggestions controller preflight, distinct from non-existence, SOC-2 CC6.6 access-denied audit trail) + 7 strongly-recommended (S1 post-seed orphan integrity probe A34: `SELECT COUNT(*) FROM knowledge_items WHERE organizationId IS NULL` returns 0; S2 replaced blocking UI-UAT with automated probes A36 positive list + A37 positive retrieval, flipped `autonomous: false→true` so plan runs end-to-end unattended; S3 suggestions.org_mismatch structured log on venue-in-org preflight fail; S4 mandatory pre-edit call-site enumeration recorded in SUMMARY.md audit trail; S5 full rollback SQL spelled out in migration header with data-loss warning; S6 positive-path probe assertions A36/A37 — catches over-restrictive scoping regressions invisible to leak-path-only tests; S7 backfill performance note for >10k rows scale). 5 deferred with triggers: D1 two-phase zero-downtime migration (Phase 4 go-live); D2 ReTagQueueItem org-scoping simplification (dedicated cleanup plan when retag queue has ops volume); D3 dedicated cross-org-access-denied API error code (external SOC-2 auditor request); D4 KnowledgeItem org-transfer flow (first tenant merger/venue reassignment); D5 per-org KnowledgeItem quota (abuse report OR public upload endpoint). Probe assertion target raised 44 → ≥52 (A30-A37). IngestService orgId guard tightened from truthy-check to UUID_RE validation — fails at the API boundary not the DB. Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix. Phase 2 | Impact: KnowledgeItem cross-org leak documented in docs.service.ts:42 closed at schema + service + retrieval + tool-input-schema layers with audit-defensible denial logs; Plan 02-02 (file upload) unblocked; patterns established for Phase 2 continuation (direct organizationId FK on any org-owned row, probe A35-style schema-shape hardening for any new Claude tool input).
- 2026-04-20 16:52: Enterprise audit on 03-03 (Typing indicator + proactive opener + multimodal image — Phase 3 closure plan). Applied 6 must-have (M1 SSRF host allowlist — isHostAllowed() validates URL host against TWILIO_MEDIA_HOST_ALLOWLIST env [default: api.twilio.com + *.s3.amazonaws.com + media.twiliocdn.com] BEFORE any fetch + redirect-target re-validation via manual-redirect follow; probe-only PROBE_MEDIA_HOST_ALLOWLIST additive + NODE_ENV!=production gated; `ssrf-rejected` reason + hostHash-never-raw in WARN log + W24 AWS-metadata-IP rejection probe; M2 MIME allowlist in download layer — ALLOWED_IMAGE_MIME_TYPES enforced BEFORE body read, `unsupported-mime` reason with specific user-facing fallback "I can only process JPEG, PNG, WebP, or GIF images." + W26 image/svg+xml rejection probe; eliminates ZodError mid-handler crash pattern; M3 magic-byte signature validator — JPEG FF D8 FF / PNG 89 50 4E 47 / GIF 47 49 46 38 / WebP RIFF...WEBP enforced post-download, `media-content-mismatch` reason + W25 100-zero-bytes rejection probe; closes content-forwarding attack vector; M4 streaming byte counter — response.body.getReader() replaces arrayBuffer() so Content-Length lies cannot bypass 5MB cap; reader.cancel() mid-stream on breach; M5 cross-tenant orgId scoping — prisma.chatConversation.findFirst proactive-opener query + pre-ChatService conversation preflight both scope `venue: { organizationId: member.organizationId }` mirroring 02-01 pattern; new AC-16 + cross_tenant_conv_mismatch WARN log; M6 typing refireCount preservation — entry retained with status='exhausted' at TYPING_MAX_REFIRES cap-hit so clearTypingRefire still returns accurate refireCount instead of 0/null; separate whatsapp.typing_indicator_exhausted log captures cap-hit for ops forensics) + 8 strongly-recommended (S1 composeOpenerText sanitization — strip [\x00-\x09\x0B-\x1F\x7F] control chars + NFC-normalize + strip WhatsApp formatting chars `*`_~\\\`\\\`\\\`` + 200-char per-line ellipsis truncation + 400-char total cap, prevents KnowledgeItem-derived text from injecting WhatsApp formatting; S2 ChatMessage.content placeholder forensic correlation — SendMessageInputSchema.attachment.sourceRef optional + placeholder format `[image: ${mediaType}, ${byteSize}B, sid:${messageSid}]`, ties conversation history row to original Twilio webhook; S3 W19 PROBE_CHAT_SERVICE_DELAY_MS 25→30s for JS GC / system load margin past 20s refire; S4 explicit top-level try/finally around 3 image servers [PORT+1 happy / PORT+3 corrupt / PORT+4 svg] with Promise.all close + wait + PROBE_MEDIA_HOST_ALLOWLIST cleanup in finally — prevents port-leak between runs; S5 SSRF-reject log uses sha256Prefix(url.host) never raw host; S6 pre-ChatService cross-tenant conversation re-verify — defense-in-depth even with M5 opener-query fix; S7 Twilio typing-indicator REST API availability decision made at PLAN time NOT discovered mid-APPLY — D-03-03-F registered up-front; ship console-mode + best-effort-REST unconditionally with fail-soft on 4xx + whatsapp.typing_indicator_unsupported_by_provider WARN; S8 PROBE_MEDIA_HOST_ALLOWLIST additive mechanism with explicit setup/teardown in probe try/finally). 5 deferred with triggers: D-03-03-F Twilio REST typing-indicator live-mode integration (trigger: Twilio docs confirm endpoint OR Meta Cloud API migration); D-03-03-G per-(venueId,userId) concurrent-inbound mutex (trigger: duplicate-opener incident OR multi-region deploy); D-03-03-H per-turn Anthropic vision cost budget cap (trigger: pre-public-launch billing review OR any venue exceeding $5/day vision spend); D-03-03-I image content moderation policy (trigger: first abuse incident OR consumer-facing deploy); D5 typing-timer persistence across restart (in-memory Map acceptable — only affects in-flight requests). AC count grew 12 → 17 (added AC-13 SSRF / AC-14 magic-byte / AC-15 MIME allowlist / AC-16 cross-tenant scope / AC-17 opener sanitization). Probe assertion target raised 23 → 27 (W18 typing immediate + W19 typing refire + W20 proactive opener happy+within-session + W21 image happy + W22 image download fail + W23 audio reject + W24 SSRF 169.254.169.254 + W25 magic-byte 100-zeros + W26 unsupported-mime svg+xml). files_modified unchanged at 9. New constants: ALLOWED_IMAGE_MIME_TYPES + DEFAULT_TWILIO_MEDIA_HOST_ALLOWLIST. Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix (sign for POC/staff-only YES; sign for public consumer surface requires closing D-03-03-G/H/I first). Phase 3 | Impact: downloadTwilioMedia is now trust-boundary-hardened at 4 layers (SSRF allowlist + MIME allowlist + magic-byte + streaming byte cap); opener-query cross-tenant leak closed before APPLY matching 02-01 SOC-2 stance; typing cap-hit observability gap closed; executor does NOT discover Twilio REST typing gap mid-task (decision made at PLAN time). Plan ready for APPLY.
- 2026-04-21 10:25: Enterprise audit on 04-01 (Broadened extraction layer — XLSX/CSV/PPTX/image-via-Claude-vision; 3 tasks, 8 ACs, standard track). Applied 4 must-have (M1 extracted-text-never-in-logs boundary across all extractors with grep-verify `rg -nE "logger\.(log\|warn\|error\|debug)\(.*(text\|content\|extracted\|base64\|data:)" apps/api/src/modules/docs/extractors/` returning zero — closes GDPR/SOC-2 CC6.1 PII-leak channel for tenant document content; M2 `sanitiseError` factored from 03-05 InfobipVerifyService into shared `apps/api/src/common/sanitise-error.ts` util + InfobipVerifyService updated to import + new image extractor imports from shared util + grep-verify no duplicate definition + zero `String(err)`/`JSON.stringify(err)` in any extractor — closes Anthropic API key leak bug-class via fetch-error serialization; M3 canary discoverability + Demo-Org-manager preconditions explicit — docs/ is git-untracked per operational PII concern, UAT operator MUST `ls docs/*.xlsx` and confirm Demo Org manager signed up via UI before AC-7, pre-APPLY-operator step not APPLY-executor step, precludes fresh-clone APPLY failures; M4 AC-7 split to 7a ingestion-hard-gate + 7b retrieval-quality-observational — multi-sheet workbooks produce single 1024-dim embedding over concatenated sheets = retrieval dilution, gating on similarity ≥0.3 would ship a plan whose PASS depends on non-deterministic embedding behavior, split makes ingestion the hard gate and records retrieval similarity in SUMMARY for 04-02 classifier-redesign signal) + 9 strongly-recommended (S1 tenant-scope boundary on image bytes explicit for both Option A/B — Option A inherits via KnowledgeItem row, Option B MUST carry organizationId + withOrgScope, defense-in-depth against future direct DocumentAsset queries; S2 MAX_CONCURRENT_IMAGE_EXTRACTS=3 semaphore with 15s queue timeout + docs.image_extract_queued log — prevents 10 parallel uploads firing 10 parallel Claude calls ≈$0.30 burst + Anthropic rate-limit trigger; S3 COMMITTED Option A [KnowledgeItem.sourceImageBytes Bytes? + sourceImageMime String?] + csv-parse/sync as defaults at plan time — removes APPLY-time A/B variability + improves reviewability; S4 sourceImageMime column named consistently across objective/output/AC-4 — removes reviewer confusion; S5 mime field added to existing docs.uploaded structured log — enables per-format regression slicing on future dashboards without retrofit; S6 mapApiError UI coverage confirmed for unsupported-mime/corrupt-bytes/timeout/empty-result user-facing error strings — prevents generic "something went wrong" fall-through on .xls rejection / magic-byte fail / extraction timeout; S7 multer 15MB single ceiling pre-per-MIME cap tradeoff acknowledged in SCOPE LIMITS + D-04-01-H registered — attacker uploading 15MB-as-jpeg wastes 15MB memory before rejection; S8 magic-byte-on-new-formats-only consistency decision documented in SCOPE LIMITS + D-04-01-I registered — existing PDF/DOCX/TXT/MD retain Phase 2 no-magic-byte behavior per DO-NOT-CHANGE invariant; S9 image-bytes object-storage migration D-04-01-F registered with concrete trigger [≥500 uploads OR ≥2GB SUM(OCTET_LENGTH(sourceImageBytes)) OR first NeonDB backup/replication slowness complaint] — storing 5MB binary blobs in Postgres TOAST is POC-acceptable but operational debt). 5 deferred with rationale (D1 prompt-injection defense on RAG corpus — project-wide, not 04-01 scope; D2 magic-byte retrofit on PDF/DOCX/TXT/MD — consistent with Phase 2 DO-NOT-CHANGE invariant; D3 locale-consistent XLSX date formatting — minor drift for embedding-based retrieval, 04-02-classifier-reconsider trigger; D4 SonarQube gate — no CI yet; D5 prod migration deployment discipline — project-wide deferred item already tracked). AC count 8 unchanged (AC-7 split modifies scope not count). Task count 3 unchanged. autonomous:true preserved — PPTX HALT remains APPLY-time emergency checkpoint pattern inherited from 03-04. Full audit at .paul/phases/04-dynamic-document-intelligence/04-01-AUDIT.md. Plan status: Updated and ready for APPLY. | Phase 4 | Impact: plan hardened for log-redaction + secret-containment + canary-discoverability + multi-sheet-embedding-tradeoff-acknowledgement; patterns established for 04-02+ to inherit (shared sanitiseError util, per-format docs.uploaded slicing, Option A image persistence as extraction-layer invariant).

- 2026-04-20 16:10: Enterprise audit on 03-02 (probe-whatsapp.ts + HMAC fixture signer + ChatService test-mode env injection — second Phase 3 plan; test-harness closes the 03-01 manual-smoke gap). Applied 4 must-have (M1 cost banner — probe makes ~34 real Claude calls = ~$0.30-1.00/run, banner surfaces this at script start before any work, plus advertises PROBE_CHAT_SERVICE_STUB=true escape hatch; M2 try/finally wrapping on ALL 3 env-modifying assertions [W10 kill-switch disabled, W11 dev-bypass positive path, W14 PROBE_CHAT_SERVICE_DELAY_MS=15000] — closes env-leak-on-throw cascade failure class; M3 venue-scoped cleanup — original cleanup only targeted channel='probe-whatsapp' marker but probe creates prod-pattern channel='whatsapp' ChatConversation rows for verified-user path, so cleanup now finds probe venues FIRST then deletes all conversations tied to venueId IN probeVenues regardless of channel; M4 dev-bypass POSITIVE-path assertion — 03-01 AC-12 had negative coverage [spawn-child prod-fail-fast] but no runtime assertion of guard accepting `X-Twilio-Signature: probe-console` when ALLOW_WEBHOOK_DEV_BYPASS=true + TWILIO_AUTH_TOKEN unset; new AC-14 + W11 closes this gap) + 8 strongly-recommended (S1 PROBE_CHAT_SERVICE_STUB=true mode — skips Claude entirely in ChatService returning deterministic fixture assistant message; production-forbidden via assertAuthEnv; enables zero-cost CI runs without forking code paths; S2 boot-time WARN via process.stderr.write when either test-mode knob active in non-prod — makes staging misconfig visible; S3 DATABASE_URL sanity check at probe start — regex rejects prod-like URLs unless DATABASE_URL_PROBE_OVERRIDE=1; S4 cleanup FK cascade order explicit inline docs — venues before orgs per onDelete:Restrict; S5 explicit `process.env.PROBE_CHAT_SERVICE_DELAY_MS='0'` reset at script start before NestFactory.create; S6 Logger capture implementation spelled out with all 5 NestJS Logger methods; S7 no-double-reply assertion post-timeout — 5s wait + recount whatsapp.outbound for specific MessageSid = 1; S8 pre-cleanup row-count assertion with 3-filter COUNT = 0 gate). 4 deferred: D1 M6 adapter constructor fail-fast probe (awkward without full-app boot); D2 multi-process horizontal test (by-design scope limit); D3 Twilio sandbox integration (Plan 03-04); D4 AbortController threading test (D-03-01-I still deferred). AC count grew 16 → 18 (new AC-1b stub-mode run, AC-14 dev-bypass positive, AC-17 pre-cleanup count). files_modified unchanged at 4. Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix. Phase 3 | Impact: probe is now CI-affordable (stub mode) + cost-transparent (banner) + leak-proof (try/finally) + hygiene-gated (pre-cleanup count); closes the dev-bypass positive-path coverage gap that 03-01 left open; patterns established for Plan 03-03 (multimodal probe) to inherit stub-mode toggle + venue-scoped cleanup.
- 2026-04-20 14:55: Enterprise audit on 03-01 (WhatsApp Inbound Webhook + Basic Text Routing — first v0.2 Phase 3 plan; public HMAC-validated webhook is entire tenant's trust boundary). Applied 7 must-have (M1 URL-pinned signature validation via `WHATSAPP_WEBHOOK_PUBLIC_URL` env — closes X-Forwarded-Host/Proto spoof attack where attacker signs request against own host + replays against real host + guard accepted it; https-only regex `^https://[^\s?#]+/webhooks/twilio/whatsapp$` + grep-verify ZERO `x-forwarded|req.get('host')` references in module; M2 dev-bypass hardening — `X-Twilio-Signature: probe-console` now requires `ALLOW_WEBHOOK_DEV_BYPASS=true` AND `NODE_ENV!=production`, assertAuthEnv fail-fast if both conditions hold in prod, WARN log on every accepted bypass not just boot; M3 MessageSid replay guard in new `seen-message-sids.ts` + ChatService 12s Promise.race hard-timeout with `whatsapp.replay_dedupe` + `whatsapp.chat_timeout` logs + ack reply — closes Twilio-retry duplicate Claude spend AND 15s webhook budget violation; M4 verified-sender per-hour rate limit 30 msgs/h in new `verified-sender-rate-limit.ts` — closes unbounded Claude cost exposure from compromised verified user; M5 ChatConversation filter fabricated `channel='whatsapp'` column REMOVED — schema has no such column; replaced with strict `{ venueId, updatedAt gte 2h-ago }` filter; D-03-01-G registered for channel column follow-up; M6 adapter constructor fail-fast when baseMode='live' + creds blank — boot-time NPE beats 3am NPE; M7 deterministic signature param iteration — reject array/non-string body values as signature-invalid, blocks qs-duplicate-key attack) + 9 strongly-recommended (S1 ChatConversation filter shape specified exactly with orgId+venueId+updatedAt; S2 orphan-Claude-promise behavior on timeout documented + D-03-01-I registered for AbortController threading; S3 env validation allows boot in disabled mode without Twilio creds; S4 WaId hashed in logs same as From/To — was phone-equivalent PII leak; S5 AC-3 second-hit rate-limit verify step added as explicit two-request curl; S6 err.message banned in logger calls — `err.constructor?.name` only, prevents PII leak via prisma query strings; S7 signature header value + URL query string banned from logs; S8 multi-org user WARN log `whatsapp.multi_org_user` + D-03-01-K User.defaultOrgId registered; S9 Zod Body.max(8000) + MessageSid/AccountSid .max(64)). 6 deferred with triggers: D-03-01-G ChatConversation channel column (first cross-channel bleed complaint OR Phase 4 analytics); D-03-01-H persistent WebhookEvent audit table (SOC2 Type II scope OR first abuse-incident reconstruction); D-03-01-I AbortController threading through ChatService.sendMessage (sustained timeout rate >1% OR Claude cost-alert); D-03-01-J per-IP rate-limit on signature-fail requests (log bloat from bad-sig spam >10k/day); D-03-01-K User.defaultOrganizationId (first multi-org user reports wrong-tenant routing); D6 TWILIO_AUTH_TOKEN rotation procedure (post-POC ops, pairs with 01-01 D5). AC count grew 7 → 12 (AC-8 replay dedupe, AC-9 cost-ceiling 30/h, AC-10 hard timeout, AC-11 URL-pin boot-fail, AC-12 dev-bypass prod-fail). files_modified grew 13 → 15 (verified-sender-rate-limit.ts + seen-message-sids.ts). Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix (sign for internal POC YES; sign for external prod NO until D-03-01-F closed). Phase 3 | Impact: public webhook endpoint now has three independent trust-boundary layers (compile-time URL-pin env requirement M1, runtime signature guard M1+M7, boot-time prod-bypass refusal M2) + cost protection (M3 idempotency + M4 rate-limit + M5 timeout); patterns established for Plan 03-02 (probe HMAC fixture signer must be URL-pin aware, all 3 rate-limiters expose `__resetForTest()` helpers).
- 2026-04-19 15:45: Enterprise audit on 01-01 (Auth + Organizations — first v0.2 plan, trust boundary for the entire milestone). Applied 10 must-have (M1 open-redirect guard via `apps/web/src/lib/safe-redirect.ts` with strict relative-path-only contract + probe P9 enforcing redirect=https://evil.example.com falls back to /chat; M2 atomic sign-up + org creation via `databaseHooks.user.create.after` hook running INSIDE the User insert transaction — REMOVED client-side `authClient.organization.create()` call that would have created a zombie-user race; probe P11 asserts rollback on hook failure; M3 migration UPDATE statements qualified `WHERE organization_id IS NULL` for idempotency; M4 seed refuses to run with `NODE_ENV=production + default DEMO_USER_PASSWORD=demo-CHANGE-me-before-prod-Xk7t9` → process.exit(1); M5 API_ERROR_CODES tuple append venue-not-found + invalid-redirect + payload-too-large + organization-slug-conflict; M6 withOrgScope generic split into typed `withOrgScope` [OrgDirectWhere union: KnowledgeItem/ChatConversation/Venue/Organization] + `withOrgScopeVia` [JoinScopedWhere: ChatMessage/MessageFeedback/ReTagQueueItem] — misuse is now compile-time error; M7 HTTP logger redaction contract — skip body + query on /api/auth/* + strip Cookie/Authorization/Set-Cookie header VALUES on ALL routes; probe A29 runtime assertion searches log capture for ProbeRedaction123 string; M8 new `apps/api/src/modules/auth/assert-auth-env.ts` called BEFORE NestFactory.create — malformed BETTER_AUTH_SECRET / BETTER_AUTH_URL / WEB_ORIGIN → fail-fast with friendly stderr message; banned `process.env.X!` non-null assertions in auth code via grep-verify; M9 /api/auth/* body cap tightened to 8 KB [NOT exempted from 32 KB global]; probe A27 asserts 413 on 16 KB payload; M10 explicit `advanced.defaultCookieAttributes: { httpOnly, sameSite: 'lax', path: '/', secure: isProd }`) + 17 strongly-recommended (S1 PasswordSchema min raised 8→12; S2 max capped at 72 for bcrypt; S3 strong demo password default; S4 `generateOrgSlug` + OrgSlugSchema kebab-case regex + 5-retry collision loop + `OrgSlugConflictError`; S5 probe-api A24 cross-org setup org slug prefix `probe-api-other-` for cleanup; S6 probe-auth cleanup pre+post mirroring v0.1 pattern; S7 new `apps/api/src/common/security-headers.middleware.ts` emits X-Content-Type-Options: nosniff + X-Frame-Options: DENY on all responses; probe A28 asserts both headers; S9 autocomplete=email/current-password/new-password/name on form fields; S10 middleware order comment block `request-id → security-headers → http-logger → auth-session-resolver → org-context` + first-log-field-order probe as regression guard; S13 research spike time-boxed to 90min with concrete success criteria [schema applies clean AND sign-up persists 3 rows AND Set-Cookie returned]; S14 rollback-warning SQL header block at top of migration file; S15 email-enumeration-safe contract — "user not found" and "wrong password" return identical `{error:'invalid-credentials'}`; probe P10 enforces; S16 trustedOrigins via assertAuthEnv's typed `env.webOrigin`; S17 probe A29 runtime body-redaction assertion). Deferred 7 (D1 account lockout → public deploy trigger; D2 persistent auth audit log table → SOC2 Type II trigger; D3 GDPR user-delete cascade → first RtBF request trigger; D4 rollback CI test → pre-launch pipeline trigger; D5 BETTER_AUTH_SECRET rotation → post-POC ops plan; D6 cross-subdomain cookie → Phase 4 Coolify; D7 session fixation explicit probe → post-POC regression). Verdict: conditionally acceptable pre-fix, enterprise-ready post-fix. Phase 1 | Impact: probe-api assertion count raised 36 → ≥42 (A23-A29); new probe-auth adds ≥11 (P1-P11 including redirect-guard + email-enum-silence + hook-rollback); files_modified grew 41 → 46 (new safe-redirect.ts, server-session.ts, assert-auth-env.ts, generate-org-slug.ts, security-headers.middleware.ts); auth trust boundary now has compile-time type safety (withOrgScope M6), runtime contract enforcement (probe A29 + P10 + P11), and boot-time fail-fast (assertAuthEnv M8) — three independent layers. Patterns established for Plan 01-02 (invitations) + 01-03 (phone linking) to inherit: atomic-hook pattern for derived rows, isSafeRedirect helper for any future redirect-accepting endpoint, email-enum-silence contract for every credential-bearing endpoint.

### Deferred Issues
- ESLint/Prettier setup (deferred from audit — add in dedicated plan or Phase 5)
- Husky/lint-staged pre-commit hooks (depends on ESLint)
- CI/CD pipeline (post-POC)
- Docker configuration (post-POC)
- Prisma `migrate deploy` strategy for production (out of POC scope; add pre-launch plan)
- Prisma query logging (`log: ['query']`) — add in dev config if Phase 3/4 needs debug visibility
- `pnpm approve-builds` — **RESOLVED** in Plan 01-02 via `pnpm.onlyBuiltDependencies`
- pnpm `save-prefix` — `pnpm add` inserts caret pins; manually reverted to `"latest"` in 02-01 and 02-02. Add `.npmrc save-prefix=""` in a future tooling plan so this enforces automatically.
- **Claude model deprecation** — **RESOLVED** post-02-02: bumped to `claude-sonnet-4-6`.
- pg v9 SSL semantics change — **RESOLVED** post-02-02: explicit `ssl` config bypasses `sslmode` aliasing.
- Incremental build cache for nest-commander CLIs — **RESOLVED** post-02-02: switched api compiler to swc (~36ms, down from ~5s).
- pgvector HNSW/IVFFlat index on `SopDocument.embedding` and `StockItem.embedding` — deferred from 03-01 audit. **Trigger:** corpus exceeds 1,000 embedded rows OR p95 retrieval latency > 500ms. Add as a schema-change plan when triggered.
- Postgres `statement_timeout` on retrieval queries — deferred from 03-01 audit. **Trigger:** corpus approaches 1,000+ rows, OR any retrieval call is observed exceeding 3,000ms in prod logs. Wire via Prisma connection-acquire hook or NestJS interceptor.

### Blockers/Concerns
- **Phase 3 intent mismatch (2026-04-18 13:10):** RESOLVED via /paul:discuss 03 on 2026-04-18. Rescoped into Agentic Knowledge Layer with KnowledgeItem + mock_* tables + agentic ingest + proactive suggestions + adaptation loop. CONTEXT.md captures goals; Plan 03-01 (rewrite) implements the schema reshape. Phase map grows to ~12 plans across 3-5 to match new intent.

## Session Continuity

Last session: 2026-04-20 21:30
Stopped at: Plan 03-04 UNIFY ✓ — loop closed. Twilio → Infobip WhatsApp provider migration structurally complete. Build clean (70 files SWC 40.75ms; `pnpm --filter @gm-ai/types build` + `pnpm --filter api build` both pass). All 13 AC assessed: 8 Pass (AC-4 typing console-only, AC-6 env block, AC-7 build clean, AC-8 Twilio Verify untouched via zero-diff, AC-9 rawBody type aug, AC-10 contact-name log drop, AC-11 middleware boot-assert, AC-13 batch deadline), 5 Structural-Pass/UAT-pending (AC-1 signature, AC-2 inbound behaviors, AC-3 outbound REST, AC-5 image download, AC-12 media auth trial). 4 deviations documented: D1 user scope-expansion mid-APPLY deleting ALL probes (probe-api, probe-auth, probe-ingest, probe-embeddings) not just probe-whatsapp — project now has zero automated regression harness, ~140 prior assertions retired; D2 AC-7 probe-api 61/61 + probe-auth 54/54 checks no longer runnable — build-clean is sole automated signal; D3 Infobip WebFetch returned only docs-hub landing page meta — proceed on reasonable-default assumptions (signature header `x-callback-signature`, hex encoding, payload `{results:[...]}`, outbound `POST /whatsapp/1/message/text`, App apiKey auth) with UAT-VERIFY citation markers + trial matrix for media URL — NOT an M4 halt because halt is for discovered drift not unverifiable assumption; D4 dead PROBE_* env hooks remain in chat.service.ts + assert-auth-env.ts + whatsapp-media-download.ts — scope creep to remove. 4 deferred items registered (D-03-04-F/G/H/I). Phase 1 Twilio Verify SMS OTP preserved (git diff apps/api/src/modules/phone/ empty). Phase 3 now 1/1 canonical plans complete (03-01/02/03 superseded historical record) — TRANSITION required for Phase 3 close → Phase 4 Coolify Deployment. Pre-Phase-4 go-live blockers: D-01-02-F, D-01-03-F, D-03-04-G, D-03-04-H + the absence of automated regression harness (recommend Vitest+supertest for API, Playwright for UI before public deploy).

Last session (prior): 2026-04-20 20:47
Stopped at: Plan 03-04 PLAN ✓ + AUDIT ✓. Enterprise audit performed 2026-04-20 20:45. Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix. Applied 7 must-have (M1 Express Request.rawBody TypeScript module augmentation at apps/api/src/types/express-augment.d.ts — closes the silent-any foot-gun if middleware refactor touches req.rawBody, M2 INFOBIP_WEBHOOK_SECRET min-length ≥32 chars validation in assertAuthEnv mirroring BETTER_AUTH_SECRET rigor, M3 signature-malformed reject BEFORE Buffer.from via hex/base64 format regex — no control-flow relies on timingSafeEqual catching malformed encoding, M4 HALT-CONDITION for APPLY-time scheme drift — if WebFetch reveals Infobip scheme materially differs from HMAC-SHA256-over-raw-body executor STOPS and raises checkpoint, M5 contact name HARD-DROP from logs — no waIdHash/contactNameHash/hashed-form; contact names are low-entropy and forensic utility is near-zero, M6 middleware-order contract + boot-time Express-stack assertion in main.ts preventing silent 403-on-everything if middleware reorders, M7 D-03-04-G registered — INFOBIP_DRIVER_OVERRIDE=console default removal as pre-Phase-4 go-live blocker mirroring D-01-03-F Twilio SMS OTP pattern) + 9 strongly-recommended (S1 raw() parser type allowlist ['application/json','application/*+json'] for Content-Type parameter tolerance, S2 signatureValidated:true flag in whatsapp.payload_invalid log — compliance audit-trail distinguishes signed-but-malformed from unsigned-garbage, S3 Infobip media-URL auth trial matrix — try App header → if 401 retry no-auth → if 401 halt+checkpoint AC-12 captures, S4 BATCH_DEADLINE_MS=12_000 soft deadline on controller for-loop preventing slow-batch Infobip retry-window starvation — AC-13 enforces, S5 INFOBIP_BASE_URL regex accepts apex + subdomain forms, S6 D-03-04-H registered for ALLOW_WEBHOOK_DEV_BYPASS post-UAT removal — retained for manual curl testing during Infobip Portal UAT only, S7 D-03-04-I registered for orphan __resetForTest exports v0.3 review, S8 enumerated 12-step Infobip Portal UAT runbook in output section — set webhook URL, join trial sender, send text, observe log signals, send image, send audio, verify AC-completion greps, S9 // Source:<URL>·verified<date> citation discipline on every WebFetch-derived fact). 5 deferred with triggers (G16 HTTP rate limiting → public-deploy, G17 Infobip outbound retry semantics → first 5xx cluster, G18 outbound idempotency keys → first duplicate-message incident, G19 guard+adapter unit tests → project-pattern deferral, G20 parallel batch processing → if Infobip batching empirically bottlenecks). AC count raised 8→13 (added AC-9 rawBody type-safe, AC-10 contact name drop, AC-11 middleware contract + boot-assert, AC-12 media-URL auth trial matrix, AC-13 batch deadline). New file added: apps/api/src/types/express-augment.d.ts. Task count unchanged (3). Full audit report at .paul/phases/03-whatsapp-integration/03-04-AUDIT.md. Plan ready for APPLY. Next routing: /paul:apply .paul/phases/03-whatsapp-integration/03-04-PLAN.md.

Last session (prior): 2026-04-20 20:39
Stopped at: Plan 03-04 PLAN.md actually authored and written to disk (prior 18:30 session captured the Infobip migration decision in Session Continuity but was cut off before the plan document itself was created — resume check caught the discrepancy). Plan 03-04 authored as deliberately simple 3-task provider swap per user instruction: no probe rewrite, no new features, just Twilio→Infobip across the WhatsApp module.

Last session (prior): 2026-04-20 18:30
Stopped at: Plan 03-04 scoped in discussion only — PLAN.md file never written. Phase 3 REOPENED for Twilio→Infobip provider migration. User decision after live-Twilio UAT prep surfaced that Twilio's API was both too convoluted AND I had hallucinated typing-indicator endpoint at 03-03 plan time. Researched Zernio (rejected — wrong abstraction layer, no inbound media / signature primitives) then Superchat (rejected — no-code platform). Settled on Infobip — tier-1 WhatsApp Business Solution Provider, free trial sender (447860088970 — Ryan's allocated trial number), HMAC-SHA256 signature, JSON inbound payload with results[] array, REST outbound at /whatsapp/1/message/text with Authorization: App {API_KEY} header. .env.example updated (Twilio WhatsApp vars removed; Twilio Verify kept for 01-03 phone OTP; Infobip vars added). .env populated with INFOBIP_BASE_URL + INFOBIP_API_KEY + INFOBIP_WHATSAPP_SENDER. Plan 03-04 preserves all 17 AC + 27 probe assertion targets from 03-03 — only the wire format changes. Plans 03-01/02/03 marked SUPERSEDED in STATE.md (PLAN.md/SUMMARY.md files preserved as historical record). Files to be modified by Plan 03-04: ~10 (rewrite adapter, signature guard, payload schema, media download module, probe-whatsapp HMAC fixture signer; preserve typing-indicator-timers + composeOpenerText + cross-tenant scoping + Phase 1/2 entirely).

Last session (prior): 2026-04-20 17:30
Stopped at: Plan 03-03 UNIFY ✓ — Phase 3 (WhatsApp Integration) CLOSED at 3/3 plans. probe-whatsapp 27/27 (idempotent on 2nd run); probe-api 61/61; probe-auth 54/54. SUMMARY at .paul/phases/03-whatsapp-integration/03-03-SUMMARY.md. 4 deviations documented: D1 Twilio typing endpoint corrected to messaging.twilio.com/v2/Indicators/Typing.json with messageId+channel (D-03-03-F partially closed); D2 PROBE_TYPING_REFIRE_MS env added to fit 12s CHAT_TIMEOUT_MS budget; D3 stub branch persists [image: ...] placeholder identically to real branch (probe-stub contract unification); D4 W6 rewritten for new image flow. Phase 3 deferred items carry: D-03-03-F (live Twilio typing UAT), D-03-03-G (concurrent-inbound mutex), D-03-03-H (vision cost cap), D-03-03-I (image moderation). Phase 1 carry-forward unchanged (D-01-02-F, D-01-03-F, AC-11/AC-10 UATs). v0.2 milestone now ~75% (10/~13 plans; 3/4 phases). Next: Phase 4 PLAN (Coolify Deployment). Files modified Phase 3: 9 (2 new + 7 modified) for Plan 03-03 alone; phase total ~30 files across 3 plans.

Last session (prior): 2026-04-20 16:52
Stopped at: Plan 03-03 CREATED + AUDITED. Enterprise audit performed 2026-04-20 16:52. Verdict: conditionally acceptable pre-fix → enterprise-ready post-fix. Applied 6 must-have (M1 SSRF host allowlist via isHostAllowed + TWILIO_MEDIA_HOST_ALLOWLIST env + PROBE_MEDIA_HOST_ALLOWLIST bypass gated on NODE_ENV!==production + redirect-target re-validation, M2 MIME allowlist ALLOWED_IMAGE_MIME_TYPES enforced in download layer with unsupported-mime reason + specific user fallback — NO more ZodError mid-handler, M3 magic-byte signature validator for JPEG/PNG/GIF/WebP with media-content-mismatch reason, M4 streaming byte counter via response.body.getReader replacing arrayBuffer so Content-Length lies can't bypass 5MB cap, M5 cross-tenant orgId scoping on proactive-opener findFirst + pre-ChatService conversation preflight with friendly fallback, M6 refireCount preservation after TYPING_MAX_REFIRES auto-stop via retained entry with status=exhausted + separate typing_indicator_exhausted log) + 8 strongly-recommended (S1 composeOpenerText sanitization — control-char strip + NFC normalize + WhatsApp formatting char strip + 200-char per-line cap, S2 ChatMessage placeholder includes sourceRef/messageSid for forensics via SendMessageInput.attachment.sourceRef, S3 W19 PROBE_CHAT_SERVICE_DELAY_MS bumped 25→30s for JS GC margin, S4 explicit try/finally on 3 image servers + PROBE_MEDIA_HOST_ALLOWLIST cleanup, S5 hostHash in SSRF-reject logs never raw URL, S6 pre-ChatService cross-tenant conversation re-verify, S7 Twilio typing-indicator REST API availability decision made at PLAN time with D-03-03-F registered — console+best-effort-REST ship unconditionally so executor doesn't stall, S8 PROBE_MEDIA_HOST_ALLOWLIST additive env mechanism). 5 deferred with triggers (D-03-03-F Twilio REST typing endpoint live-mode — revisit on Twilio docs confirm OR Cloud API migration; D-03-03-G per-user concurrent-inbound mutex — POC bound, revisit on incident or multi-region; D-03-03-H per-turn Anthropic vision cost cap — pre-public-launch billing review; D-03-03-I image content moderation policy — first abuse incident OR consumer-facing deploy; timer persistence across restart — in-memory Map acceptable). AC count raised 12→17 (added AC-13 SSRF, AC-14 magic-byte, AC-15 MIME allowlist, AC-16 cross-tenant scope, AC-17 sanitization). Probe assertion target raised 23→27 (added W24 SSRF-reject 169.254.169.254 test, W25 magic-byte mismatch with 100 zero bytes, W26 unsupported-mime with image/svg+xml). Files: 9 modified unchanged in count. New constants shipped: TYPING_REFIRE_MS=20s + TYPING_MAX_REFIRES=6 + PROACTIVE_SESSION_WINDOW_MS=24h + MAX_IMAGE_DOWNLOAD_BYTES=5MB + MEDIA_DOWNLOAD_TIMEOUT_MS=5s + ALLOWED_IMAGE_MIME_TYPES (audit-added) + DEFAULT_TWILIO_MEDIA_HOST_ALLOWLIST (audit-added). Plan ready for APPLY. Next routing: /paul:apply .paul/phases/03-whatsapp-integration/03-03-PLAN.md.

Phase 3 progress: 2 of 3-4 est plans complete; 03-03 plan complete pre-audit. Option (d) closes Phase 3 ENGINEERING in one plan — only live-Twilio-sandbox UAT remains as a separate manual checkpoint. Real-Claude-mode probe-whatsapp run (unset stub, ~$1 spend) still recommended before release tagging. If sandbox UAT becomes a formal plan, it would be 03-04.

Phase 1 exit-gate items still carried forward (UI-only UATs and pre-Phase-4 go-live blockers): AC-11 /settings/phone walk (01-03); AC-10 cross-org isolation walk (01-01); AC-10 invitation flow walk (01-02); D-01-02-F real email-verification flow; D-01-03-F strip TWILIO_DRIVER_OVERRIDE=console default; D-03-01-F remove probe-console dev-bypass branch + ALLOW_WEBHOOK_DEV_BYPASS env var. Deferred: D-03-01-H (WebhookEvent audit table), D-03-01-I (AbortController threading), D-03-01-J (per-IP rate limit on signature-fail), D-03-01-K (User.defaultOrganizationId). D-03-01-G CLOSED. Plan 03-02's PROBE_CHAT_SERVICE_DELAY_MS + PROBE_CHAT_SERVICE_STUB auto-guarded by assertAuthEnv prod-fail. Plan 03-03 adds potential D-03-03-F if Twilio WhatsApp typing-indicator REST API isn't available — in which case typing ships console-mode-only with live-mode flagged for sandbox UAT discovery.

Next action: /paul:audit .paul/phases/03-whatsapp-integration/03-04-PLAN.md (recommended — enterprise audit enabled in config) OR /paul:apply .paul/phases/03-whatsapp-integration/03-04-PLAN.md to skip audit.
Resume file: .paul/phases/03-whatsapp-integration/03-04-PLAN.md

---

### Prior session notes (Plan 02-02 UNIFY + Phase 2 closure — 2026-04-20 12:32)

Plan 02-02 fully closed (PLAN ✓ + AUDIT ✓ + APPLY ✓ + UNIFY ✓). SUMMARY at .paul/phases/02-document-ingest/02-02-SUMMARY.md. 5 commits: `8e2782e plan`, `de08845 audit`, `4c426da Task 1`, `63fe1b8 Task 2`, `ac7aceb Task 3`, `c71154f UNIFY`. probe-api 61/61 + probe-auth 54/54 green. Manager self-serve document surface end-to-end (upload + list + get + delete) complete. SOC-2 CC6.6 audit trail symmetric on read + write + delete paths (docs.uploaded + docs.cross_org_denied with op discriminator).

Plan 02-01 fully closed prior (6 commits: `ede6ee9 plan`, `a5e728f audit`, `9eea65c Task 1`, `1c175ec Task 2`, `13bfe74 Task 3`, `e511727 UNIFY`). probe-api 52/52 → 61/61 + probe-auth 54/54 green. SOC-2 CC6.6 cross-org leak on global KnowledgeItem rows closed.

---

### Prior session notes (Plan 01-03 UNIFY — 2026-04-20 10:58)

Prior session stopped at: Plan 01-03 (Phone Linking via Twilio Verify) UNIFY ✓ — loop closed. SUMMARY.md at .paul/phases/01-auth-organizations/01-03-SUMMARY.md. Phase 1 (Auth + Organizations) closes at 3/3 plans. v0.2.0 milestone progresses 17% → 25%. Single commit shipped: `4419a0f plan 01-03 — phone linking via twilio verify` (17 files changed: 8 new + 9 modified).

Phase 1 exit-gate outstanding items (carry forward):
  - **AC-11 UI UAT (01-03)** — BLOCKER for Phase 1 retrospective closure. Two-step send→verify flow + unlink confirmation dialog walk at /settings/phone. Human-only; not reachable by probe.
  - **AC-10 UAT carry-forward from 01-01** — multi-profile cross-org isolation walk still outstanding.
  - **AC-10 UAT carry-forward from 01-02** — 12-step invitation flow walk still outstanding.
  - **D-01-02-F** — actual email-verification flow removing the dev bypass on invitation M2 gate. Pre-Phase-4 go-live BLOCKER.
  - **D-01-03-F** (new) — remove TWILIO_DRIVER_OVERRIDE=console default from .env.example before Phase 4 go-live. Current default is probe-safe but production-unsafe (prod would ship in console-mode with zero outbound SMS).

Next action: Phase 2 (Document Ingest UI) PLAN. Run `/paul:plan` to scope first Phase 2 plan. Recommended: execute the 3 outstanding UATs (01-01 cross-org, 01-02 invitation, 01-03 phone) before Phase 2 APPLY to surface UX drift the probes miss. Phase 2 plan-count estimated at 2-3; first plan likely "02-01 manager document upload (POST /ingest/documents + multipart + MIME validation + org-scoping on KnowledgeItem)".

Resume file: .paul/phases/01-auth-organizations/01-03-SUMMARY.md

---

### Prior session notes (Plan 01-03 PLAN + AUDIT + APPLY — 2026-04-19 20:54 → 2026-04-20 10:58)

Prior session stopped at: Plan 01-02 (Invitations) UNIFY ✓. SUMMARY.md written at .paul/phases/01-auth-organizations/01-02-SUMMARY.md. All 12 AC assessed: 10 Pass (code), AC-9 partial (builds clean, probe:api regression run blocked by pre-existing DB state), AC-10 deferred (UAT not yet performed this session), AC-7/AC-8 probe runtime deferred. Phase 1 progresses from 1/3 → 2/3 plans complete (67%).

Working-tree state after 01-02 UNIFY:
  - UNCOMMITTED: Plan 01-01 Tasks 2+3 (auth module + UI; 9+10 files), Phase 2 docs work (docs module + UI + save_knowledge_doc + conversation list + Document schema + system-prompt.ts + chat/debug body extractions), Plan 01-02 entire scope (14 new + 10 modified files), + @radix-ui/react-dialog dep in package.json.
  - Three plans' worth of code in a single tree. Splitting strongly advised before next commit — recommended segmentation: (a) "plan 01-01 tasks 2+3 — nestjs auth module + next.js auth ui" (b) "phase 2 docs + save_knowledge_doc + conversation-list" as retroactive 02-01 (c) "plan 01-02 — invitations". OR a single squash commit with trailer listing all three plan refs.

Cumulative pre-01-03 prerequisites:
  1. Commit hygiene (above).
  2. `pnpm seed` — repopulates Demo Org venues (probe:api blocks on "No venues seeded in Demo Org" — current DB state).
  3. Sign up DEMO_USER_EMAIL via UI (01-01 Deviation: Demo User cannot be created by seed because better-auth owns password hashing).
  4. Run `pnpm --filter api probe:api` — must pass ≥42 assertions (close 01-01 AC-7 + 01-02 AC-9 regression check).
  5. Run `pnpm --filter api probe:auth` — must pass ≥24 assertions (close 01-01 P1–P11 + 01-02 P12–P21 + audit-added P13b/P16b/P17b/P18).
  6. Execute 01-01 AC-9 UAT (multi-profile cross-org isolation walk).
  7. Execute 01-02 AC-10 UAT (12-step invitation flow walk).

Critical go-live-gate carry-forward from 01-02 audit:
  - D-01-02-F: actual email-verification flow (magic-link or OTP) removing the dev bypass on M2 emailVerified gate MUST be wired before Phase 4 Coolify public deploy. Tag as blocker on Phase 4 go-live checklist.

Next action: Run `/paul:audit 01-03` (enterprise audit enabled in config.md) then `/paul:apply 01-03`. Prerequisites remain outstanding and compound risk on APPLY — strong recommendation to address commit hygiene + seed + UAT + probe runs BEFORE APPLY lands more code in the same tree.
Resume file: .paul/phases/01-auth-organizations/01-03-PLAN.md
Audit report (pending): .paul/phases/01-auth-organizations/01-03-AUDIT.md

---

### Prior session notes (Plan 01-02 PLAN + AUDIT — 2026-04-19 17:45–18:00)

Plan 01-02 (Invitations) PLAN ✓ + AUDIT ✓. Plan hardened with 10 must-have + 8 strongly-recommended upgrades; 7 items deferred with explicit triggers. probe-auth target raised ≥17 → ≥24 assertions. 2 new AC added (AC-11 per-org cap + AC-12 pagination). API_ERROR_CODES grew by 9 entries. Full audit trail in .paul/phases/01-auth-organizations/01-02-AUDIT.md.

---

### Prior session notes (Plan 01-01 UNIFY — 2026-04-19 17:30)

Plan 01-01 UNIFY complete. SUMMARY.md written at .paul/phases/01-auth-organizations/01-01-SUMMARY.md. All 10 AC assessed: 8 Pass, AC-7 partial (probe-auth.ts created but not yet re-run post-consolidation), AC-9 deferred (UAT not yet performed). Phase 1 progresses from 0/3 → 1/3 plans complete.

Working-tree state after UNIFY:
  - Tasks 2 (NestJS auth module + probes) and 3 (Next.js UI) code landed in repo but NOT YET COMMITTED. Atomic-commit-per-task hygiene broken by cross-session split.
  - Out-of-scope Phase 2 work mixed into the same working tree: apps/api/src/modules/docs/, apps/web/src/app/docs/, packages/types/src/docs.ts, save_knowledge_doc tool (chat-tools.ts), conversation list API + hooks, chat-body.tsx/debug-body.tsx extractions, system-prompt.ts refinements. These need splitting into a separate commit (ideally with a retroactive Phase 2 plan).

Pre-01-02 prerequisites:
  1. Split working tree by concern; commit Plan 01-01 Tasks 2+3 atomically (one commit per task if practical, or bundled "plan 01-01 tasks 2+3 — auth module + ui").
  2. Commit Phase 2 docs work separately (or create retroactive plan 02-01 stub + SUMMARY).
  3. Run `pnpm --filter api probe:auth` + `pnpm --filter api probe:api` regression — both must pass post-consolidation.
  4. Execute AC-9 UAT via `/paul:verify` — sign up ryan@ryanhelmn.dev via UI, confirm chat loop still works, second browser profile confirms cross-org isolation.

Next action: Run `/paul:plan` to scope Plan 01-02 (invitation flow) OR pause to address commit hygiene + UAT first. Recommended: UAT + commits before 01-02.
Resume file: .paul/phases/01-auth-organizations/01-01-SUMMARY.md

---

### Prior session notes (Plan 01-01 Task 1 outcomes — 2026-04-19 16:20)

Task 1 concrete outcomes:
  - ADAPTER DECISION: native Prisma adapter chosen. @better-auth/prisma-adapter@1.6.5 peer deps officially include @prisma/client: ^7.0.0 — no Kysely fallback needed. Spike took ~5 min vs 90-min cap.
  - MIGRATION: 20260419160000_auth_orgs applied to live Neon dev DB. Hand-crafted SQL with rollback-warning header (S14), inline Demo Organization INSERT with ON CONFLICT DO NOTHING, two-step Venue.organizationId (ADD NULL → UPDATE WHERE IS NULL → SET NOT NULL) pattern M3, all 7 new tables + indexes + FKs. migrate status: Database schema is up to date.
  - 7 new Prisma models: User, Session, Account, VerificationToken, Organization, OrganizationMember, Invitation + Venue.organizationId NOT NULL onDelete:Restrict.
  - @gm-ai/types new auth.ts with Role/AuthUser/AuthSession/AuthOrganization/AuthMembership types + EmailSchema/PasswordSchema(min 12 max 72 for bcrypt S1+S2)/NameSchema/OrgNameSchema/OrgSlugSchema(S4)/SignUpBodySchema/SignInBodySchema. API_ERROR_CODES appended: unauthorized, forbidden, email-already-registered, invalid-credentials, organization-not-found, member-not-found, invalid-redirect, payload-too-large, organization-slug-conflict (M5). Build clean.
  - seed-data.ts: DEMO_ORG_ID/NAME/SLUG constants. seed.command.ts: assertSeedSafe() prod-safe guard M4, Demo Org upsert, organizationId=DEMO_ORG_ID on venue upserts.
  - apps/api/scripts/migrate-phase-1-data.ts: idempotent backfill; guards production; updateMany WHERE organizationId != DEMO_ORG_ID.
  - Deps: better-auth@1.6.5, kysely@0.28.16, @better-auth/cli@1.4.21(dev). apps/api/package.json: probe:auth + migrate:phase-1-data scripts.
  - .env.example: v0.2 Phase 1 section appended (BETTER_AUTH_SECRET, BETTER_AUTH_URL, DEMO_USER_EMAIL, DEMO_USER_PASSWORD=demo-CHANGE-me-before-prod-Xk7t9, SEED_DEMO) + WEB_ORIGIN added (was missing from v0.1 despite plan claim).
  - .env: BETTER_AUTH_SECRET generated (64 hex chars), all Phase 1 keys populated.
  - `pnpm --filter api build` clean (45 files swc 44ms). `pnpm --filter @gm-ai/types build` clean.

Deviations to fold into SUMMARY in UNIFY:
  - seed files live at apps/api/src/modules/seed/ not apps/api/src/seed/ (plan had wrong path)
  - VOYAGE_API_KEY is the real env name (not VOYAGEAI_API_KEY as plan audit claimed)
  - WEB_ORIGIN / NEXT_PUBLIC_API_URL / ANTHROPIC_MODEL not in root .env.example from v0.1 (plan audit assumed they were). WEB_ORIGIN added by this task because M8 assertAuthEnv requires it.
  - Demo User creation deferred to sign-up flow (better-auth owns password hashing; direct bcrypt insert would break signin). UAT Step 6 revised: "sign up ryan@ryanhelmn.dev first via UI before signing in." Retrofit to Demo Org membership will need a post-signup admin migration OR a custom afterCreate hook special-case for DEMO_USER_EMAIL.

Next action: Resume in fresh session with `/paul:resume`. Resume work at Task 2 (NestJS AuthModule + guards + controller retrofits + probe extensions) and Task 3 (Next.js auth UI). Full audited plan at .paul/phases/01-auth-organizations/01-01-PLAN.md. Audit report at .paul/phases/01-auth-organizations/01-01-AUDIT.md. Remaining: ~25 new files + 5 controller retrofits + 2 probe scripts + 10 UI files + UAT.
Resume file: .paul/phases/01-auth-organizations/01-01-PLAN.md
Audit report: .paul/phases/01-auth-organizations/01-01-AUDIT.md

### Git State
Last commit: 7191967 — plan 01-01 task 1 — auth + org schema, migration, types, seed
Tags: v0.1.0 (annotated, on 7aba52d)
Branch: main
Working tree: UNCOMMITTED after UNIFY. Contains:
  - Plan 01-01 Tasks 2+3: apps/api/src/modules/auth/* (9 files), apps/api/src/common/{with-org-scope,security-headers.middleware}.ts, apps/api/src/scripts/probe-auth.ts, apps/api/src/main.ts + common/http-logger.middleware.ts mods, controller retrofits (chat/suggestions/feedback/venues/debug), apps/web/src/lib/{auth-client,safe-redirect,server-session}.ts, apps/web/src/components/auth/*, apps/web/src/app/auth/*, new shadcn ui primitives.
  - Out-of-scope Phase 2 docs work: apps/api/src/modules/docs/, apps/web/src/app/docs/, apps/web/src/components/docs/, packages/types/src/docs.ts, apps/web/src/lib/hooks/use-{docs,conversations-list}.ts, chat-body.tsx/debug-body.tsx extractions, system-prompt.ts refinements, save_knowledge_doc tool wired into chat-tools.ts, schema.prisma Document model.
  - User MUST split these before next commit. Recommended: (a) commit "plan 01-01 tasks 2+3 — auth module + next.js auth ui", (b) separately handle Phase 2 work via retroactive Plan 02-01 stub + its own commit.

Milestone commit trail (v0.1 POC):
- 51af306 — Phase 1 (scaffold + prisma schema)
- 88ab109 — plan 02-01 (embeddings service)
- 11e6049 — plan 02-02 (seeder + claude enrichment)
- 1abd945 — post-phase-2 concerns (swc, claude-sonnet-4-6, pg ssl)
- ceb81bb — Phase 3 (agentic knowledge + ingest + retrieval)
- 3569f16 — Phase 4 (chat engine + suggestions + adaptation)
- 9efb5a5 — plan 05-01 (api controllers + zodPipe + probe-api)
- fe88a8a — plan 05-02 (chat ui)
- a12c78a — plan 05-03 + Phase 5 transition (debug panel + aggregate docs)

---
*STATE.md — Updated after every significant action*
