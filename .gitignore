# Dependencies
node_modules/

# Build outputs
dist/
.next/
out/
*.tsbuildinfo

# Canary fixtures — POS exports + beerhall xlsx are test inputs only,
# never to be tracked (real venue revenue / payroll / customer data).
docs/*.xlsx
docs/*.csv
docs/*.tsv

# Turbo
.turbo/

# Environment
.env
.env.local
.env.*.local

# Prisma
packages/database/src/generated/

# Swagger / OpenAPI codegen — emitted by `pnpm --filter api swagger:generate`,
# consumed by orval on the web side. Re-generate on demand; not committed.
apps/api/swagger.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
pnpm-debug.log*
.paul/automation-logs/
